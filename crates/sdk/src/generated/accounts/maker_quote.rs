//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use solana_address::Address;
use borsh::BorshSerialize;
use borsh::BorshDeserialize;


#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct MakerQuote {
pub discriminator: u8,
pub maker: Address,
pub market: Address,
pub delegate: Address,
pub quote_id_le: [u8; 8],
pub sequence_le: [u8; 8],
pub mid_tick_le: [u8; 4],
pub last_update_slot_le: [u8; 8],
pub expiry_slots_le: [u8; 8],
pub folded_auction_id_le: [u8; 8],
pub settled_auction_id_le: [u8; 8],
pub num_bids: u8,
pub num_asks: u8,
pub status: u8,
pub bump: u8,
pub bid_levels_le: [u8; 80],
pub ask_levels_le: [u8; 80],
pub bid_snapshots_le: [u8; 64],
pub ask_snapshots_le: [u8; 64],
}


pub const MAKER_QUOTE_DISCRIMINATOR: u8 = 8;

impl MakerQuote {
  
          /// Prefix values used to generate a PDA for this account.
    ///
    /// Values are positional and appear in the following order:
    ///
                  ///   0. `MakerQuote::PREFIX`
                                ///   1. market (`Address`)
                        ///   2. maker (`Address`)
                    pub const PREFIX: &'static [u8] = "maker_quote".as_bytes();
      
      pub fn create_pda(
                                                                market: Address,
                                                maker: Address,
                                  bump: u8,
    ) -> Result<solana_address::Address, solana_address::error::AddressError> {
      solana_address::Address::create_program_address(
        &[
                                    "maker_quote".as_bytes(),
                                                market.as_ref(),
                                                maker.as_ref(),
                                &[bump],
        ],
        &crate::TEMPO_PROGRAM_ID,
      )
    }

    pub fn find_pda(
                                                    market: &Address,
                                        maker: &Address,
                          ) -> (solana_address::Address, u8) {
      solana_address::Address::find_program_address(
        &[
                                    "maker_quote".as_bytes(),
                                                market.as_ref(),
                                                maker.as_ref(),
                              ],
        &crate::TEMPO_PROGRAM_ID,
      )
    }
  
  #[inline(always)]
  pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
    let mut data = data;
    Self::deserialize(&mut data)
  }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for MakerQuote {
  type Error = std::io::Error;

  fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
      let mut data: &[u8] = &(*account_info.data).borrow();
      Self::deserialize(&mut data)
  }
}

#[cfg(feature = "fetch")]
pub fn fetch_maker_quote(
  rpc: &solana_rpc_client::rpc_client::RpcClient,
  address: &solana_address::Address,
) -> Result<crate::shared::DecodedAccount<MakerQuote>, std::io::Error> {
  let accounts = fetch_all_maker_quote(rpc, &[*address])?;
  Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maker_quote(
  rpc: &solana_rpc_client::rpc_client::RpcClient,
  addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::DecodedAccount<MakerQuote>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<MakerQuote>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      let account = accounts[i].as_ref()
        .ok_or(std::io::Error::other(format!("Account not found: {address}")))?;
      let data = MakerQuote::from_bytes(&account.data)?;
      decoded_accounts.push(crate::shared::DecodedAccount { address, account: account.clone(), data });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_maker_quote(
  rpc: &solana_rpc_client::rpc_client::RpcClient,
  address: &solana_address::Address,
) -> Result<crate::shared::MaybeAccount<MakerQuote>, std::io::Error> {
    let accounts = fetch_all_maybe_maker_quote(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_maker_quote(
  rpc: &solana_rpc_client::rpc_client::RpcClient,
  addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::MaybeAccount<MakerQuote>>, std::io::Error> {
    let accounts = rpc.get_multiple_accounts(addresses)
      .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<MakerQuote>> = Vec::new();
    for i in 0..addresses.len() {
      let address = addresses[i];
      if let Some(account) = accounts[i].as_ref() {
        let data = MakerQuote::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::MaybeAccount::Exists(crate::shared::DecodedAccount { address, account: account.clone(), data }));
      } else {
        decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
      }
    }
  Ok(decoded_accounts)
}

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountDeserialize for MakerQuote {
      fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
      }
  }

  #[cfg(feature = "anchor")]
  impl anchor_lang::AccountSerialize for MakerQuote {}

  #[cfg(feature = "anchor")]
  impl anchor_lang::Owner for MakerQuote {
      fn owner() -> anchor_lang::solana_program::pubkey::Pubkey {
        anchor_lang::solana_program::pubkey::Pubkey::from(
          crate::TEMPO_PROGRAM_ID.to_bytes()
        )
      }
  }

  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::IdlBuild for MakerQuote {}

  
  #[cfg(feature = "anchor-idl-build")]
  impl anchor_lang::Discriminator for MakerQuote {
    const DISCRIMINATOR: &[u8] = &[0; 8];
  }


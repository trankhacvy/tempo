//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const UPDATE_MARKET_PARAMS_DISCRIMINATOR: u8 = 33;

/// Accounts.
#[derive(Debug)]
pub struct UpdateMarketParams {
    /// Market authority
    pub authority: solana_address::Address,
    /// Market to retune
    pub market: solana_address::Address,
    /// Event authority PDA for CPI event emission
    pub event_authority: solana_address::Address,
    /// Tempo program, for self-CPI event emission
    pub tempo_program: solana_address::Address,
}

impl UpdateMarketParams {
    pub fn instruction(
        &self,
        args: UpdateMarketParamsInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: UpdateMarketParamsInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.authority,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.market, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.event_authority,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.tempo_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = UpdateMarketParamsInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::TEMPO_PROGRAM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct UpdateMarketParamsInstructionData {
    discriminator: u8,
}

impl UpdateMarketParamsInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 33 }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for UpdateMarketParamsInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct UpdateMarketParamsInstructionArgs {
    pub maker_fee_bps: i16,
    pub taker_fee_bps: i16,
    pub integrator_share_bps: u16,
    pub crank_fee: u64,
    pub max_price_move_bps_per_slot: u16,
    pub soft_stale_slots: u64,
    pub max_position_notional: u128,
    pub min_order_notional: u64,
    pub max_open_interest: u128,
    pub liquidation_reward_floor: u64,
}

impl UpdateMarketParamsInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `UpdateMarketParams`.
///
/// ### Accounts:
///
///   0. `[signer]` authority
///   1. `[writable]` market
///   2. `[]` event_authority
///   3. `[]` tempo_program
#[derive(Clone, Debug, Default)]
pub struct UpdateMarketParamsBuilder {
    authority: Option<solana_address::Address>,
    market: Option<solana_address::Address>,
    event_authority: Option<solana_address::Address>,
    tempo_program: Option<solana_address::Address>,
    maker_fee_bps: Option<i16>,
    taker_fee_bps: Option<i16>,
    integrator_share_bps: Option<u16>,
    crank_fee: Option<u64>,
    max_price_move_bps_per_slot: Option<u16>,
    soft_stale_slots: Option<u64>,
    max_position_notional: Option<u128>,
    min_order_notional: Option<u64>,
    max_open_interest: Option<u128>,
    liquidation_reward_floor: Option<u64>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl UpdateMarketParamsBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Market authority
    #[inline(always)]
    pub fn authority(&mut self, authority: solana_address::Address) -> &mut Self {
        self.authority = Some(authority);
        self
    }
    /// Market to retune
    #[inline(always)]
    pub fn market(&mut self, market: solana_address::Address) -> &mut Self {
        self.market = Some(market);
        self
    }
    /// Event authority PDA for CPI event emission
    #[inline(always)]
    pub fn event_authority(&mut self, event_authority: solana_address::Address) -> &mut Self {
        self.event_authority = Some(event_authority);
        self
    }
    /// Tempo program, for self-CPI event emission
    #[inline(always)]
    pub fn tempo_program(&mut self, tempo_program: solana_address::Address) -> &mut Self {
        self.tempo_program = Some(tempo_program);
        self
    }
    #[inline(always)]
    pub fn maker_fee_bps(&mut self, maker_fee_bps: i16) -> &mut Self {
        self.maker_fee_bps = Some(maker_fee_bps);
        self
    }
    #[inline(always)]
    pub fn taker_fee_bps(&mut self, taker_fee_bps: i16) -> &mut Self {
        self.taker_fee_bps = Some(taker_fee_bps);
        self
    }
    #[inline(always)]
    pub fn integrator_share_bps(&mut self, integrator_share_bps: u16) -> &mut Self {
        self.integrator_share_bps = Some(integrator_share_bps);
        self
    }
    #[inline(always)]
    pub fn crank_fee(&mut self, crank_fee: u64) -> &mut Self {
        self.crank_fee = Some(crank_fee);
        self
    }
    #[inline(always)]
    pub fn max_price_move_bps_per_slot(&mut self, max_price_move_bps_per_slot: u16) -> &mut Self {
        self.max_price_move_bps_per_slot = Some(max_price_move_bps_per_slot);
        self
    }
    #[inline(always)]
    pub fn soft_stale_slots(&mut self, soft_stale_slots: u64) -> &mut Self {
        self.soft_stale_slots = Some(soft_stale_slots);
        self
    }
    #[inline(always)]
    pub fn max_position_notional(&mut self, max_position_notional: u128) -> &mut Self {
        self.max_position_notional = Some(max_position_notional);
        self
    }
    #[inline(always)]
    pub fn min_order_notional(&mut self, min_order_notional: u64) -> &mut Self {
        self.min_order_notional = Some(min_order_notional);
        self
    }
    #[inline(always)]
    pub fn max_open_interest(&mut self, max_open_interest: u128) -> &mut Self {
        self.max_open_interest = Some(max_open_interest);
        self
    }
    #[inline(always)]
    pub fn liquidation_reward_floor(&mut self, liquidation_reward_floor: u64) -> &mut Self {
        self.liquidation_reward_floor = Some(liquidation_reward_floor);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = UpdateMarketParams {
            authority: self.authority.expect("authority is not set"),
            market: self.market.expect("market is not set"),
            event_authority: self.event_authority.expect("event_authority is not set"),
            tempo_program: self.tempo_program.expect("tempo_program is not set"),
        };
        let args = UpdateMarketParamsInstructionArgs {
            maker_fee_bps: self
                .maker_fee_bps
                .clone()
                .expect("maker_fee_bps is not set"),
            taker_fee_bps: self
                .taker_fee_bps
                .clone()
                .expect("taker_fee_bps is not set"),
            integrator_share_bps: self
                .integrator_share_bps
                .clone()
                .expect("integrator_share_bps is not set"),
            crank_fee: self.crank_fee.clone().expect("crank_fee is not set"),
            max_price_move_bps_per_slot: self
                .max_price_move_bps_per_slot
                .clone()
                .expect("max_price_move_bps_per_slot is not set"),
            soft_stale_slots: self
                .soft_stale_slots
                .clone()
                .expect("soft_stale_slots is not set"),
            max_position_notional: self
                .max_position_notional
                .clone()
                .expect("max_position_notional is not set"),
            min_order_notional: self
                .min_order_notional
                .clone()
                .expect("min_order_notional is not set"),
            max_open_interest: self
                .max_open_interest
                .clone()
                .expect("max_open_interest is not set"),
            liquidation_reward_floor: self
                .liquidation_reward_floor
                .clone()
                .expect("liquidation_reward_floor is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `update_market_params` CPI accounts.
pub struct UpdateMarketParamsCpiAccounts<'a, 'b> {
    /// Market authority
    pub authority: &'b solana_account_info::AccountInfo<'a>,
    /// Market to retune
    pub market: &'b solana_account_info::AccountInfo<'a>,
    /// Event authority PDA for CPI event emission
    pub event_authority: &'b solana_account_info::AccountInfo<'a>,
    /// Tempo program, for self-CPI event emission
    pub tempo_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `update_market_params` CPI instruction.
pub struct UpdateMarketParamsCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Market authority
    pub authority: &'b solana_account_info::AccountInfo<'a>,
    /// Market to retune
    pub market: &'b solana_account_info::AccountInfo<'a>,
    /// Event authority PDA for CPI event emission
    pub event_authority: &'b solana_account_info::AccountInfo<'a>,
    /// Tempo program, for self-CPI event emission
    pub tempo_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: UpdateMarketParamsInstructionArgs,
}

impl<'a, 'b> UpdateMarketParamsCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: UpdateMarketParamsCpiAccounts<'a, 'b>,
        args: UpdateMarketParamsInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            authority: accounts.authority,
            market: accounts.market,
            event_authority: accounts.event_authority,
            tempo_program: accounts.tempo_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.authority.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.market.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.event_authority.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.tempo_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = UpdateMarketParamsInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::TEMPO_PROGRAM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(5 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.authority.clone());
        account_infos.push(self.market.clone());
        account_infos.push(self.event_authority.clone());
        account_infos.push(self.tempo_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `UpdateMarketParams` via CPI.
///
/// ### Accounts:
///
///   0. `[signer]` authority
///   1. `[writable]` market
///   2. `[]` event_authority
///   3. `[]` tempo_program
#[derive(Clone, Debug)]
pub struct UpdateMarketParamsCpiBuilder<'a, 'b> {
    instruction: Box<UpdateMarketParamsCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> UpdateMarketParamsCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(UpdateMarketParamsCpiBuilderInstruction {
            __program: program,
            authority: None,
            market: None,
            event_authority: None,
            tempo_program: None,
            maker_fee_bps: None,
            taker_fee_bps: None,
            integrator_share_bps: None,
            crank_fee: None,
            max_price_move_bps_per_slot: None,
            soft_stale_slots: None,
            max_position_notional: None,
            min_order_notional: None,
            max_open_interest: None,
            liquidation_reward_floor: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Market authority
    #[inline(always)]
    pub fn authority(&mut self, authority: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.authority = Some(authority);
        self
    }
    /// Market to retune
    #[inline(always)]
    pub fn market(&mut self, market: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.market = Some(market);
        self
    }
    /// Event authority PDA for CPI event emission
    #[inline(always)]
    pub fn event_authority(
        &mut self,
        event_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.event_authority = Some(event_authority);
        self
    }
    /// Tempo program, for self-CPI event emission
    #[inline(always)]
    pub fn tempo_program(
        &mut self,
        tempo_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.tempo_program = Some(tempo_program);
        self
    }
    #[inline(always)]
    pub fn maker_fee_bps(&mut self, maker_fee_bps: i16) -> &mut Self {
        self.instruction.maker_fee_bps = Some(maker_fee_bps);
        self
    }
    #[inline(always)]
    pub fn taker_fee_bps(&mut self, taker_fee_bps: i16) -> &mut Self {
        self.instruction.taker_fee_bps = Some(taker_fee_bps);
        self
    }
    #[inline(always)]
    pub fn integrator_share_bps(&mut self, integrator_share_bps: u16) -> &mut Self {
        self.instruction.integrator_share_bps = Some(integrator_share_bps);
        self
    }
    #[inline(always)]
    pub fn crank_fee(&mut self, crank_fee: u64) -> &mut Self {
        self.instruction.crank_fee = Some(crank_fee);
        self
    }
    #[inline(always)]
    pub fn max_price_move_bps_per_slot(&mut self, max_price_move_bps_per_slot: u16) -> &mut Self {
        self.instruction.max_price_move_bps_per_slot = Some(max_price_move_bps_per_slot);
        self
    }
    #[inline(always)]
    pub fn soft_stale_slots(&mut self, soft_stale_slots: u64) -> &mut Self {
        self.instruction.soft_stale_slots = Some(soft_stale_slots);
        self
    }
    #[inline(always)]
    pub fn max_position_notional(&mut self, max_position_notional: u128) -> &mut Self {
        self.instruction.max_position_notional = Some(max_position_notional);
        self
    }
    #[inline(always)]
    pub fn min_order_notional(&mut self, min_order_notional: u64) -> &mut Self {
        self.instruction.min_order_notional = Some(min_order_notional);
        self
    }
    #[inline(always)]
    pub fn max_open_interest(&mut self, max_open_interest: u128) -> &mut Self {
        self.instruction.max_open_interest = Some(max_open_interest);
        self
    }
    #[inline(always)]
    pub fn liquidation_reward_floor(&mut self, liquidation_reward_floor: u64) -> &mut Self {
        self.instruction.liquidation_reward_floor = Some(liquidation_reward_floor);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = UpdateMarketParamsInstructionArgs {
            maker_fee_bps: self
                .instruction
                .maker_fee_bps
                .clone()
                .expect("maker_fee_bps is not set"),
            taker_fee_bps: self
                .instruction
                .taker_fee_bps
                .clone()
                .expect("taker_fee_bps is not set"),
            integrator_share_bps: self
                .instruction
                .integrator_share_bps
                .clone()
                .expect("integrator_share_bps is not set"),
            crank_fee: self
                .instruction
                .crank_fee
                .clone()
                .expect("crank_fee is not set"),
            max_price_move_bps_per_slot: self
                .instruction
                .max_price_move_bps_per_slot
                .clone()
                .expect("max_price_move_bps_per_slot is not set"),
            soft_stale_slots: self
                .instruction
                .soft_stale_slots
                .clone()
                .expect("soft_stale_slots is not set"),
            max_position_notional: self
                .instruction
                .max_position_notional
                .clone()
                .expect("max_position_notional is not set"),
            min_order_notional: self
                .instruction
                .min_order_notional
                .clone()
                .expect("min_order_notional is not set"),
            max_open_interest: self
                .instruction
                .max_open_interest
                .clone()
                .expect("max_open_interest is not set"),
            liquidation_reward_floor: self
                .instruction
                .liquidation_reward_floor
                .clone()
                .expect("liquidation_reward_floor is not set"),
        };
        let instruction = UpdateMarketParamsCpi {
            __program: self.instruction.__program,

            authority: self.instruction.authority.expect("authority is not set"),

            market: self.instruction.market.expect("market is not set"),

            event_authority: self
                .instruction
                .event_authority
                .expect("event_authority is not set"),

            tempo_program: self
                .instruction
                .tempo_program
                .expect("tempo_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct UpdateMarketParamsCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    market: Option<&'b solana_account_info::AccountInfo<'a>>,
    event_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    tempo_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    maker_fee_bps: Option<i16>,
    taker_fee_bps: Option<i16>,
    integrator_share_bps: Option<u16>,
    crank_fee: Option<u64>,
    max_price_move_bps_per_slot: Option<u16>,
    soft_stale_slots: Option<u64>,
    max_position_notional: Option<u128>,
    min_order_notional: Option<u64>,
    max_open_interest: Option<u128>,
    liquidation_reward_floor: Option<u64>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

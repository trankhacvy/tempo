//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const MIGRATE_MARKET_DISCRIMINATOR: u8 = 26;

/// Accounts.
#[derive(Debug)]
pub struct MigrateMarket {
    /// Market authority (admin)
    pub authority: solana_address::Address,
    /// Market account to upgrade
    pub market: solana_address::Address,
    /// Funds the grown-account rent
    pub payer: solana_address::Address,
    /// System program
    pub system_program: solana_address::Address,
}

impl MigrateMarket {
    pub fn instruction(
        &self,
        args: MigrateMarketInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: MigrateMarketInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.authority,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.market, false));
        accounts.push(solana_instruction::AccountMeta::new(self.payer, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = MigrateMarketInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::TEMPO_PROGRAM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct MigrateMarketInstructionData {
    discriminator: u8,
}

impl MigrateMarketInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 26 }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for MigrateMarketInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct MigrateMarketInstructionArgs {
    pub max_price_move_bps_per_slot: u16,
    pub soft_stale_slots: u64,
}

impl MigrateMarketInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `MigrateMarket`.
///
/// ### Accounts:
///
///   0. `[signer]` authority
///   1. `[writable]` market
///   2. `[writable, signer]` payer
///   3. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct MigrateMarketBuilder {
    authority: Option<solana_address::Address>,
    market: Option<solana_address::Address>,
    payer: Option<solana_address::Address>,
    system_program: Option<solana_address::Address>,
    max_price_move_bps_per_slot: Option<u16>,
    soft_stale_slots: Option<u64>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl MigrateMarketBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Market authority (admin)
    #[inline(always)]
    pub fn authority(&mut self, authority: solana_address::Address) -> &mut Self {
        self.authority = Some(authority);
        self
    }
    /// Market account to upgrade
    #[inline(always)]
    pub fn market(&mut self, market: solana_address::Address) -> &mut Self {
        self.market = Some(market);
        self
    }
    /// Funds the grown-account rent
    #[inline(always)]
    pub fn payer(&mut self, payer: solana_address::Address) -> &mut Self {
        self.payer = Some(payer);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    /// System program
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_address::Address) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn max_price_move_bps_per_slot(&mut self, max_price_move_bps_per_slot: u16) -> &mut Self {
        self.max_price_move_bps_per_slot = Some(max_price_move_bps_per_slot);
        self
    }
    #[inline(always)]
    pub fn soft_stale_slots(&mut self, soft_stale_slots: u64) -> &mut Self {
        self.soft_stale_slots = Some(soft_stale_slots);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = MigrateMarket {
            authority: self.authority.expect("authority is not set"),
            market: self.market.expect("market is not set"),
            payer: self.payer.expect("payer is not set"),
            system_program: self
                .system_program
                .unwrap_or(solana_address::address!("11111111111111111111111111111111")),
        };
        let args = MigrateMarketInstructionArgs {
            max_price_move_bps_per_slot: self
                .max_price_move_bps_per_slot
                .clone()
                .expect("max_price_move_bps_per_slot is not set"),
            soft_stale_slots: self
                .soft_stale_slots
                .clone()
                .expect("soft_stale_slots is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `migrate_market` CPI accounts.
pub struct MigrateMarketCpiAccounts<'a, 'b> {
    /// Market authority (admin)
    pub authority: &'b solana_account_info::AccountInfo<'a>,
    /// Market account to upgrade
    pub market: &'b solana_account_info::AccountInfo<'a>,
    /// Funds the grown-account rent
    pub payer: &'b solana_account_info::AccountInfo<'a>,
    /// System program
    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `migrate_market` CPI instruction.
pub struct MigrateMarketCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Market authority (admin)
    pub authority: &'b solana_account_info::AccountInfo<'a>,
    /// Market account to upgrade
    pub market: &'b solana_account_info::AccountInfo<'a>,
    /// Funds the grown-account rent
    pub payer: &'b solana_account_info::AccountInfo<'a>,
    /// System program
    pub system_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: MigrateMarketInstructionArgs,
}

impl<'a, 'b> MigrateMarketCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: MigrateMarketCpiAccounts<'a, 'b>,
        args: MigrateMarketInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            authority: accounts.authority,
            market: accounts.market,
            payer: accounts.payer,
            system_program: accounts.system_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.authority.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.market.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.payer.key, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = MigrateMarketInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::TEMPO_PROGRAM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(5 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.authority.clone());
        account_infos.push(self.market.clone());
        account_infos.push(self.payer.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `MigrateMarket` via CPI.
///
/// ### Accounts:
///
///   0. `[signer]` authority
///   1. `[writable]` market
///   2. `[writable, signer]` payer
///   3. `[]` system_program
#[derive(Clone, Debug)]
pub struct MigrateMarketCpiBuilder<'a, 'b> {
    instruction: Box<MigrateMarketCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> MigrateMarketCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(MigrateMarketCpiBuilderInstruction {
            __program: program,
            authority: None,
            market: None,
            payer: None,
            system_program: None,
            max_price_move_bps_per_slot: None,
            soft_stale_slots: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Market authority (admin)
    #[inline(always)]
    pub fn authority(&mut self, authority: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.authority = Some(authority);
        self
    }
    /// Market account to upgrade
    #[inline(always)]
    pub fn market(&mut self, market: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.market = Some(market);
        self
    }
    /// Funds the grown-account rent
    #[inline(always)]
    pub fn payer(&mut self, payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.payer = Some(payer);
        self
    }
    /// System program
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn max_price_move_bps_per_slot(&mut self, max_price_move_bps_per_slot: u16) -> &mut Self {
        self.instruction.max_price_move_bps_per_slot = Some(max_price_move_bps_per_slot);
        self
    }
    #[inline(always)]
    pub fn soft_stale_slots(&mut self, soft_stale_slots: u64) -> &mut Self {
        self.instruction.soft_stale_slots = Some(soft_stale_slots);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = MigrateMarketInstructionArgs {
            max_price_move_bps_per_slot: self
                .instruction
                .max_price_move_bps_per_slot
                .clone()
                .expect("max_price_move_bps_per_slot is not set"),
            soft_stale_slots: self
                .instruction
                .soft_stale_slots
                .clone()
                .expect("soft_stale_slots is not set"),
        };
        let instruction = MigrateMarketCpi {
            __program: self.instruction.__program,

            authority: self.instruction.authority.expect("authority is not set"),

            market: self.instruction.market.expect("market is not set"),

            payer: self.instruction.payer.expect("payer is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct MigrateMarketCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    market: Option<&'b solana_account_info::AccountInfo<'a>>,
    payer: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    max_price_move_bps_per_slot: Option<u16>,
    soft_stale_slots: Option<u64>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

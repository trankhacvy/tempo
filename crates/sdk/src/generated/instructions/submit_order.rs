//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const SUBMIT_ORDER_DISCRIMINATOR: u8 = 1;

/// Accounts.
#[derive(Debug)]
pub struct SubmitOrder {
    /// Order owner
    pub trader: solana_address::Address,
    /// Market the order belongs to. Writable (Stage A): the first order into an empty shard bumps `shards_pending`.
    pub market: solana_address::Address,
    /// OrderSlab SHARD to insert into. Stage A sharding: a market has num_slab_shards slabs at seeds [b"order_slab", market, shard_id.to_le_bytes()]. The client resolves the shard PDA for the chosen `shard_id` (least-full / hash) and passes it here; the processor validates the PDA against `shard_id`.
    pub order_slab: solana_address::Address,
    /// Event authority PDA for CPI event emission
    pub event_authority: solana_address::Address,
    /// Tempo program, for self-CPI event emission
    pub tempo_program: solana_address::Address,
    /// (Optional) trader's Position; required on a money-path market to reserve margin
    pub position: Option<solana_address::Address>,
    /// (Optional) trader's collateral ledger; locks the order's worst-case initial margin
    pub user_collateral: Option<solana_address::Address>,
}

impl SubmitOrder {
    pub fn instruction(&self, args: SubmitOrderInstructionArgs) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: SubmitOrderInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(7 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.trader, true));
        accounts.push(solana_instruction::AccountMeta::new(self.market, false));
        accounts.push(solana_instruction::AccountMeta::new(self.order_slab, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.event_authority,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.tempo_program,
            false,
        ));
        if let Some(position) = self.position {
            accounts.push(solana_instruction::AccountMeta::new(position, false));
        }
        if let Some(user_collateral) = self.user_collateral {
            accounts.push(solana_instruction::AccountMeta::new(user_collateral, false));
        }
        accounts.extend_from_slice(remaining_accounts);
        let mut data = SubmitOrderInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::TEMPO_PROGRAM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct SubmitOrderInstructionData {
    discriminator: u8,
}

impl SubmitOrderInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 1 }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for SubmitOrderInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct SubmitOrderInstructionArgs {
    pub side: u8,
    pub price: u64,
    pub quantity: u64,
    pub reduce_only: bool,
    pub shard_id: u16,
}

impl SubmitOrderInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `SubmitOrder`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` trader
///   1. `[writable]` market
///   2. `[writable]` order_slab
///   3. `[]` event_authority
///   4. `[]` tempo_program
///   5. `[writable, optional]` position
///   6. `[writable, optional]` user_collateral
#[derive(Clone, Debug, Default)]
pub struct SubmitOrderBuilder {
    trader: Option<solana_address::Address>,
    market: Option<solana_address::Address>,
    order_slab: Option<solana_address::Address>,
    event_authority: Option<solana_address::Address>,
    tempo_program: Option<solana_address::Address>,
    position: Option<solana_address::Address>,
    user_collateral: Option<solana_address::Address>,
    side: Option<u8>,
    price: Option<u64>,
    quantity: Option<u64>,
    reduce_only: Option<bool>,
    shard_id: Option<u16>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl SubmitOrderBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Order owner
    #[inline(always)]
    pub fn trader(&mut self, trader: solana_address::Address) -> &mut Self {
        self.trader = Some(trader);
        self
    }
    /// Market the order belongs to. Writable (Stage A): the first order into an empty shard bumps `shards_pending`.
    #[inline(always)]
    pub fn market(&mut self, market: solana_address::Address) -> &mut Self {
        self.market = Some(market);
        self
    }
    /// OrderSlab SHARD to insert into. Stage A sharding: a market has num_slab_shards slabs at seeds [b"order_slab", market, shard_id.to_le_bytes()]. The client resolves the shard PDA for the chosen `shard_id` (least-full / hash) and passes it here; the processor validates the PDA against `shard_id`.
    #[inline(always)]
    pub fn order_slab(&mut self, order_slab: solana_address::Address) -> &mut Self {
        self.order_slab = Some(order_slab);
        self
    }
    /// Event authority PDA for CPI event emission
    #[inline(always)]
    pub fn event_authority(&mut self, event_authority: solana_address::Address) -> &mut Self {
        self.event_authority = Some(event_authority);
        self
    }
    /// Tempo program, for self-CPI event emission
    #[inline(always)]
    pub fn tempo_program(&mut self, tempo_program: solana_address::Address) -> &mut Self {
        self.tempo_program = Some(tempo_program);
        self
    }
    /// `[optional account]`
    /// (Optional) trader's Position; required on a money-path market to reserve margin
    #[inline(always)]
    pub fn position(&mut self, position: Option<solana_address::Address>) -> &mut Self {
        self.position = position;
        self
    }
    /// `[optional account]`
    /// (Optional) trader's collateral ledger; locks the order's worst-case initial margin
    #[inline(always)]
    pub fn user_collateral(
        &mut self,
        user_collateral: Option<solana_address::Address>,
    ) -> &mut Self {
        self.user_collateral = user_collateral;
        self
    }
    #[inline(always)]
    pub fn side(&mut self, side: u8) -> &mut Self {
        self.side = Some(side);
        self
    }
    #[inline(always)]
    pub fn price(&mut self, price: u64) -> &mut Self {
        self.price = Some(price);
        self
    }
    #[inline(always)]
    pub fn quantity(&mut self, quantity: u64) -> &mut Self {
        self.quantity = Some(quantity);
        self
    }
    #[inline(always)]
    pub fn reduce_only(&mut self, reduce_only: bool) -> &mut Self {
        self.reduce_only = Some(reduce_only);
        self
    }
    #[inline(always)]
    pub fn shard_id(&mut self, shard_id: u16) -> &mut Self {
        self.shard_id = Some(shard_id);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = SubmitOrder {
            trader: self.trader.expect("trader is not set"),
            market: self.market.expect("market is not set"),
            order_slab: self.order_slab.expect("order_slab is not set"),
            event_authority: self.event_authority.expect("event_authority is not set"),
            tempo_program: self.tempo_program.expect("tempo_program is not set"),
            position: self.position,
            user_collateral: self.user_collateral,
        };
        let args = SubmitOrderInstructionArgs {
            side: self.side.clone().expect("side is not set"),
            price: self.price.clone().expect("price is not set"),
            quantity: self.quantity.clone().expect("quantity is not set"),
            reduce_only: self.reduce_only.clone().expect("reduce_only is not set"),
            shard_id: self.shard_id.clone().expect("shard_id is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `submit_order` CPI accounts.
pub struct SubmitOrderCpiAccounts<'a, 'b> {
    /// Order owner
    pub trader: &'b solana_account_info::AccountInfo<'a>,
    /// Market the order belongs to. Writable (Stage A): the first order into an empty shard bumps `shards_pending`.
    pub market: &'b solana_account_info::AccountInfo<'a>,
    /// OrderSlab SHARD to insert into. Stage A sharding: a market has num_slab_shards slabs at seeds [b"order_slab", market, shard_id.to_le_bytes()]. The client resolves the shard PDA for the chosen `shard_id` (least-full / hash) and passes it here; the processor validates the PDA against `shard_id`.
    pub order_slab: &'b solana_account_info::AccountInfo<'a>,
    /// Event authority PDA for CPI event emission
    pub event_authority: &'b solana_account_info::AccountInfo<'a>,
    /// Tempo program, for self-CPI event emission
    pub tempo_program: &'b solana_account_info::AccountInfo<'a>,
    /// (Optional) trader's Position; required on a money-path market to reserve margin
    pub position: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// (Optional) trader's collateral ledger; locks the order's worst-case initial margin
    pub user_collateral: Option<&'b solana_account_info::AccountInfo<'a>>,
}

/// `submit_order` CPI instruction.
pub struct SubmitOrderCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Order owner
    pub trader: &'b solana_account_info::AccountInfo<'a>,
    /// Market the order belongs to. Writable (Stage A): the first order into an empty shard bumps `shards_pending`.
    pub market: &'b solana_account_info::AccountInfo<'a>,
    /// OrderSlab SHARD to insert into. Stage A sharding: a market has num_slab_shards slabs at seeds [b"order_slab", market, shard_id.to_le_bytes()]. The client resolves the shard PDA for the chosen `shard_id` (least-full / hash) and passes it here; the processor validates the PDA against `shard_id`.
    pub order_slab: &'b solana_account_info::AccountInfo<'a>,
    /// Event authority PDA for CPI event emission
    pub event_authority: &'b solana_account_info::AccountInfo<'a>,
    /// Tempo program, for self-CPI event emission
    pub tempo_program: &'b solana_account_info::AccountInfo<'a>,
    /// (Optional) trader's Position; required on a money-path market to reserve margin
    pub position: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// (Optional) trader's collateral ledger; locks the order's worst-case initial margin
    pub user_collateral: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// The arguments for the instruction.
    pub __args: SubmitOrderInstructionArgs,
}

impl<'a, 'b> SubmitOrderCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: SubmitOrderCpiAccounts<'a, 'b>,
        args: SubmitOrderInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            trader: accounts.trader,
            market: accounts.market,
            order_slab: accounts.order_slab,
            event_authority: accounts.event_authority,
            tempo_program: accounts.tempo_program,
            position: accounts.position,
            user_collateral: accounts.user_collateral,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(7 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.trader.key, true));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.market.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.order_slab.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.event_authority.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.tempo_program.key,
            false,
        ));
        if let Some(position) = self.position {
            accounts.push(solana_instruction::AccountMeta::new(*position.key, false));
        }
        if let Some(user_collateral) = self.user_collateral {
            accounts.push(solana_instruction::AccountMeta::new(
                *user_collateral.key,
                false,
            ));
        }
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = SubmitOrderInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::TEMPO_PROGRAM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(8 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.trader.clone());
        account_infos.push(self.market.clone());
        account_infos.push(self.order_slab.clone());
        account_infos.push(self.event_authority.clone());
        account_infos.push(self.tempo_program.clone());
        if let Some(position) = self.position {
            account_infos.push(position.clone());
        }
        if let Some(user_collateral) = self.user_collateral {
            account_infos.push(user_collateral.clone());
        }
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `SubmitOrder` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` trader
///   1. `[writable]` market
///   2. `[writable]` order_slab
///   3. `[]` event_authority
///   4. `[]` tempo_program
///   5. `[writable, optional]` position
///   6. `[writable, optional]` user_collateral
#[derive(Clone, Debug)]
pub struct SubmitOrderCpiBuilder<'a, 'b> {
    instruction: Box<SubmitOrderCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> SubmitOrderCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(SubmitOrderCpiBuilderInstruction {
            __program: program,
            trader: None,
            market: None,
            order_slab: None,
            event_authority: None,
            tempo_program: None,
            position: None,
            user_collateral: None,
            side: None,
            price: None,
            quantity: None,
            reduce_only: None,
            shard_id: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Order owner
    #[inline(always)]
    pub fn trader(&mut self, trader: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.trader = Some(trader);
        self
    }
    /// Market the order belongs to. Writable (Stage A): the first order into an empty shard bumps `shards_pending`.
    #[inline(always)]
    pub fn market(&mut self, market: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.market = Some(market);
        self
    }
    /// OrderSlab SHARD to insert into. Stage A sharding: a market has num_slab_shards slabs at seeds [b"order_slab", market, shard_id.to_le_bytes()]. The client resolves the shard PDA for the chosen `shard_id` (least-full / hash) and passes it here; the processor validates the PDA against `shard_id`.
    #[inline(always)]
    pub fn order_slab(
        &mut self,
        order_slab: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.order_slab = Some(order_slab);
        self
    }
    /// Event authority PDA for CPI event emission
    #[inline(always)]
    pub fn event_authority(
        &mut self,
        event_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.event_authority = Some(event_authority);
        self
    }
    /// Tempo program, for self-CPI event emission
    #[inline(always)]
    pub fn tempo_program(
        &mut self,
        tempo_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.tempo_program = Some(tempo_program);
        self
    }
    /// `[optional account]`
    /// (Optional) trader's Position; required on a money-path market to reserve margin
    #[inline(always)]
    pub fn position(
        &mut self,
        position: Option<&'b solana_account_info::AccountInfo<'a>>,
    ) -> &mut Self {
        self.instruction.position = position;
        self
    }
    /// `[optional account]`
    /// (Optional) trader's collateral ledger; locks the order's worst-case initial margin
    #[inline(always)]
    pub fn user_collateral(
        &mut self,
        user_collateral: Option<&'b solana_account_info::AccountInfo<'a>>,
    ) -> &mut Self {
        self.instruction.user_collateral = user_collateral;
        self
    }
    #[inline(always)]
    pub fn side(&mut self, side: u8) -> &mut Self {
        self.instruction.side = Some(side);
        self
    }
    #[inline(always)]
    pub fn price(&mut self, price: u64) -> &mut Self {
        self.instruction.price = Some(price);
        self
    }
    #[inline(always)]
    pub fn quantity(&mut self, quantity: u64) -> &mut Self {
        self.instruction.quantity = Some(quantity);
        self
    }
    #[inline(always)]
    pub fn reduce_only(&mut self, reduce_only: bool) -> &mut Self {
        self.instruction.reduce_only = Some(reduce_only);
        self
    }
    #[inline(always)]
    pub fn shard_id(&mut self, shard_id: u16) -> &mut Self {
        self.instruction.shard_id = Some(shard_id);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = SubmitOrderInstructionArgs {
            side: self.instruction.side.clone().expect("side is not set"),
            price: self.instruction.price.clone().expect("price is not set"),
            quantity: self
                .instruction
                .quantity
                .clone()
                .expect("quantity is not set"),
            reduce_only: self
                .instruction
                .reduce_only
                .clone()
                .expect("reduce_only is not set"),
            shard_id: self
                .instruction
                .shard_id
                .clone()
                .expect("shard_id is not set"),
        };
        let instruction = SubmitOrderCpi {
            __program: self.instruction.__program,

            trader: self.instruction.trader.expect("trader is not set"),

            market: self.instruction.market.expect("market is not set"),

            order_slab: self.instruction.order_slab.expect("order_slab is not set"),

            event_authority: self
                .instruction
                .event_authority
                .expect("event_authority is not set"),

            tempo_program: self
                .instruction
                .tempo_program
                .expect("tempo_program is not set"),

            position: self.instruction.position,

            user_collateral: self.instruction.user_collateral,
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct SubmitOrderCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    trader: Option<&'b solana_account_info::AccountInfo<'a>>,
    market: Option<&'b solana_account_info::AccountInfo<'a>>,
    order_slab: Option<&'b solana_account_info::AccountInfo<'a>>,
    event_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    tempo_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    position: Option<&'b solana_account_info::AccountInfo<'a>>,
    user_collateral: Option<&'b solana_account_info::AccountInfo<'a>>,
    side: Option<u8>,
    price: Option<u64>,
    quantity: Option<u64>,
    reduce_only: Option<bool>,
    shard_id: Option<u16>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

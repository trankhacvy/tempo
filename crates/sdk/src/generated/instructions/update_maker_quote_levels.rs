//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const UPDATE_MAKER_QUOTE_LEVELS_DISCRIMINATOR: u8 = 18;

/// Accounts.
#[derive(Debug)]
pub struct UpdateMakerQuoteLevels {
    /// Maker or its delegate
    pub writer: solana_address::Address,
    /// Market (supplies num_ticks)
    pub market: solana_address::Address,
    /// MakerQuote to update
    pub maker_quote: solana_address::Address,
    /// (Optional) the MAKER's mint-scoped collateral ledger (reservation delta-locked here); REQUIRED on a money-path market, omitted on clearing-only
    pub user_collateral: Option<solana_address::Address>,
}

impl UpdateMakerQuoteLevels {
    pub fn instruction(
        &self,
        args: UpdateMakerQuoteLevelsInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: UpdateMakerQuoteLevelsInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.writer,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.market,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.maker_quote,
            false,
        ));
        if let Some(user_collateral) = self.user_collateral {
            accounts.push(solana_instruction::AccountMeta::new(user_collateral, false));
        }
        accounts.extend_from_slice(remaining_accounts);
        let mut data = UpdateMakerQuoteLevelsInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::TEMPO_PROGRAM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct UpdateMakerQuoteLevelsInstructionData {
    discriminator: u8,
}

impl UpdateMakerQuoteLevelsInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 18 }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for UpdateMakerQuoteLevelsInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct UpdateMakerQuoteLevelsInstructionArgs {
    pub sequence: u64,
    pub mid_tick: u32,
    pub num_bids: u8,
    pub num_asks: u8,
    pub bid_levels: [u8; 80],
    pub ask_levels: [u8; 80],
}

impl UpdateMakerQuoteLevelsInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `UpdateMakerQuoteLevels`.
///
/// ### Accounts:
///
///   0. `[signer]` writer
///   1. `[]` market
///   2. `[writable]` maker_quote
///   3. `[writable, optional]` user_collateral
#[derive(Clone, Debug, Default)]
pub struct UpdateMakerQuoteLevelsBuilder {
    writer: Option<solana_address::Address>,
    market: Option<solana_address::Address>,
    maker_quote: Option<solana_address::Address>,
    user_collateral: Option<solana_address::Address>,
    sequence: Option<u64>,
    mid_tick: Option<u32>,
    num_bids: Option<u8>,
    num_asks: Option<u8>,
    bid_levels: Option<[u8; 80]>,
    ask_levels: Option<[u8; 80]>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl UpdateMakerQuoteLevelsBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Maker or its delegate
    #[inline(always)]
    pub fn writer(&mut self, writer: solana_address::Address) -> &mut Self {
        self.writer = Some(writer);
        self
    }
    /// Market (supplies num_ticks)
    #[inline(always)]
    pub fn market(&mut self, market: solana_address::Address) -> &mut Self {
        self.market = Some(market);
        self
    }
    /// MakerQuote to update
    #[inline(always)]
    pub fn maker_quote(&mut self, maker_quote: solana_address::Address) -> &mut Self {
        self.maker_quote = Some(maker_quote);
        self
    }
    /// `[optional account]`
    /// (Optional) the MAKER's mint-scoped collateral ledger (reservation delta-locked here); REQUIRED on a money-path market, omitted on clearing-only
    #[inline(always)]
    pub fn user_collateral(
        &mut self,
        user_collateral: Option<solana_address::Address>,
    ) -> &mut Self {
        self.user_collateral = user_collateral;
        self
    }
    #[inline(always)]
    pub fn sequence(&mut self, sequence: u64) -> &mut Self {
        self.sequence = Some(sequence);
        self
    }
    #[inline(always)]
    pub fn mid_tick(&mut self, mid_tick: u32) -> &mut Self {
        self.mid_tick = Some(mid_tick);
        self
    }
    #[inline(always)]
    pub fn num_bids(&mut self, num_bids: u8) -> &mut Self {
        self.num_bids = Some(num_bids);
        self
    }
    #[inline(always)]
    pub fn num_asks(&mut self, num_asks: u8) -> &mut Self {
        self.num_asks = Some(num_asks);
        self
    }
    #[inline(always)]
    pub fn bid_levels(&mut self, bid_levels: [u8; 80]) -> &mut Self {
        self.bid_levels = Some(bid_levels);
        self
    }
    #[inline(always)]
    pub fn ask_levels(&mut self, ask_levels: [u8; 80]) -> &mut Self {
        self.ask_levels = Some(ask_levels);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = UpdateMakerQuoteLevels {
            writer: self.writer.expect("writer is not set"),
            market: self.market.expect("market is not set"),
            maker_quote: self.maker_quote.expect("maker_quote is not set"),
            user_collateral: self.user_collateral,
        };
        let args = UpdateMakerQuoteLevelsInstructionArgs {
            sequence: self.sequence.clone().expect("sequence is not set"),
            mid_tick: self.mid_tick.clone().expect("mid_tick is not set"),
            num_bids: self.num_bids.clone().expect("num_bids is not set"),
            num_asks: self.num_asks.clone().expect("num_asks is not set"),
            bid_levels: self.bid_levels.clone().expect("bid_levels is not set"),
            ask_levels: self.ask_levels.clone().expect("ask_levels is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `update_maker_quote_levels` CPI accounts.
pub struct UpdateMakerQuoteLevelsCpiAccounts<'a, 'b> {
    /// Maker or its delegate
    pub writer: &'b solana_account_info::AccountInfo<'a>,
    /// Market (supplies num_ticks)
    pub market: &'b solana_account_info::AccountInfo<'a>,
    /// MakerQuote to update
    pub maker_quote: &'b solana_account_info::AccountInfo<'a>,
    /// (Optional) the MAKER's mint-scoped collateral ledger (reservation delta-locked here); REQUIRED on a money-path market, omitted on clearing-only
    pub user_collateral: Option<&'b solana_account_info::AccountInfo<'a>>,
}

/// `update_maker_quote_levels` CPI instruction.
pub struct UpdateMakerQuoteLevelsCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Maker or its delegate
    pub writer: &'b solana_account_info::AccountInfo<'a>,
    /// Market (supplies num_ticks)
    pub market: &'b solana_account_info::AccountInfo<'a>,
    /// MakerQuote to update
    pub maker_quote: &'b solana_account_info::AccountInfo<'a>,
    /// (Optional) the MAKER's mint-scoped collateral ledger (reservation delta-locked here); REQUIRED on a money-path market, omitted on clearing-only
    pub user_collateral: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// The arguments for the instruction.
    pub __args: UpdateMakerQuoteLevelsInstructionArgs,
}

impl<'a, 'b> UpdateMakerQuoteLevelsCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: UpdateMakerQuoteLevelsCpiAccounts<'a, 'b>,
        args: UpdateMakerQuoteLevelsInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            writer: accounts.writer,
            market: accounts.market,
            maker_quote: accounts.maker_quote,
            user_collateral: accounts.user_collateral,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.writer.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.market.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.maker_quote.key,
            false,
        ));
        if let Some(user_collateral) = self.user_collateral {
            accounts.push(solana_instruction::AccountMeta::new(
                *user_collateral.key,
                false,
            ));
        }
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = UpdateMakerQuoteLevelsInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::TEMPO_PROGRAM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(5 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.writer.clone());
        account_infos.push(self.market.clone());
        account_infos.push(self.maker_quote.clone());
        if let Some(user_collateral) = self.user_collateral {
            account_infos.push(user_collateral.clone());
        }
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `UpdateMakerQuoteLevels` via CPI.
///
/// ### Accounts:
///
///   0. `[signer]` writer
///   1. `[]` market
///   2. `[writable]` maker_quote
///   3. `[writable, optional]` user_collateral
#[derive(Clone, Debug)]
pub struct UpdateMakerQuoteLevelsCpiBuilder<'a, 'b> {
    instruction: Box<UpdateMakerQuoteLevelsCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> UpdateMakerQuoteLevelsCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(UpdateMakerQuoteLevelsCpiBuilderInstruction {
            __program: program,
            writer: None,
            market: None,
            maker_quote: None,
            user_collateral: None,
            sequence: None,
            mid_tick: None,
            num_bids: None,
            num_asks: None,
            bid_levels: None,
            ask_levels: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Maker or its delegate
    #[inline(always)]
    pub fn writer(&mut self, writer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.writer = Some(writer);
        self
    }
    /// Market (supplies num_ticks)
    #[inline(always)]
    pub fn market(&mut self, market: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.market = Some(market);
        self
    }
    /// MakerQuote to update
    #[inline(always)]
    pub fn maker_quote(
        &mut self,
        maker_quote: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.maker_quote = Some(maker_quote);
        self
    }
    /// `[optional account]`
    /// (Optional) the MAKER's mint-scoped collateral ledger (reservation delta-locked here); REQUIRED on a money-path market, omitted on clearing-only
    #[inline(always)]
    pub fn user_collateral(
        &mut self,
        user_collateral: Option<&'b solana_account_info::AccountInfo<'a>>,
    ) -> &mut Self {
        self.instruction.user_collateral = user_collateral;
        self
    }
    #[inline(always)]
    pub fn sequence(&mut self, sequence: u64) -> &mut Self {
        self.instruction.sequence = Some(sequence);
        self
    }
    #[inline(always)]
    pub fn mid_tick(&mut self, mid_tick: u32) -> &mut Self {
        self.instruction.mid_tick = Some(mid_tick);
        self
    }
    #[inline(always)]
    pub fn num_bids(&mut self, num_bids: u8) -> &mut Self {
        self.instruction.num_bids = Some(num_bids);
        self
    }
    #[inline(always)]
    pub fn num_asks(&mut self, num_asks: u8) -> &mut Self {
        self.instruction.num_asks = Some(num_asks);
        self
    }
    #[inline(always)]
    pub fn bid_levels(&mut self, bid_levels: [u8; 80]) -> &mut Self {
        self.instruction.bid_levels = Some(bid_levels);
        self
    }
    #[inline(always)]
    pub fn ask_levels(&mut self, ask_levels: [u8; 80]) -> &mut Self {
        self.instruction.ask_levels = Some(ask_levels);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = UpdateMakerQuoteLevelsInstructionArgs {
            sequence: self
                .instruction
                .sequence
                .clone()
                .expect("sequence is not set"),
            mid_tick: self
                .instruction
                .mid_tick
                .clone()
                .expect("mid_tick is not set"),
            num_bids: self
                .instruction
                .num_bids
                .clone()
                .expect("num_bids is not set"),
            num_asks: self
                .instruction
                .num_asks
                .clone()
                .expect("num_asks is not set"),
            bid_levels: self
                .instruction
                .bid_levels
                .clone()
                .expect("bid_levels is not set"),
            ask_levels: self
                .instruction
                .ask_levels
                .clone()
                .expect("ask_levels is not set"),
        };
        let instruction = UpdateMakerQuoteLevelsCpi {
            __program: self.instruction.__program,

            writer: self.instruction.writer.expect("writer is not set"),

            market: self.instruction.market.expect("market is not set"),

            maker_quote: self
                .instruction
                .maker_quote
                .expect("maker_quote is not set"),

            user_collateral: self.instruction.user_collateral,
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct UpdateMakerQuoteLevelsCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    writer: Option<&'b solana_account_info::AccountInfo<'a>>,
    market: Option<&'b solana_account_info::AccountInfo<'a>>,
    maker_quote: Option<&'b solana_account_info::AccountInfo<'a>>,
    user_collateral: Option<&'b solana_account_info::AccountInfo<'a>>,
    sequence: Option<u64>,
    mid_tick: Option<u32>,
    num_bids: Option<u8>,
    num_asks: Option<u8>,
    bid_levels: Option<[u8; 80]>,
    ask_levels: Option<[u8; 80]>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const ACCEPT_AUTHORITY_TRANSFER_DISCRIMINATOR: u8 = 37;

/// Accounts.
#[derive(Debug)]
pub struct AcceptAuthorityTransfer {
    /// The staged new authority
    pub new_authority: solana_address::Address,
    /// Market whose authority rotates
    pub market: solana_address::Address,
    /// Event authority PDA for CPI event emission
    pub event_authority: solana_address::Address,
    /// Tempo program, for self-CPI event emission
    pub tempo_program: solana_address::Address,
}

impl AcceptAuthorityTransfer {
    pub fn instruction(&self) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.new_authority,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.market, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.event_authority,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.tempo_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let data = AcceptAuthorityTransferInstructionData::new()
            .try_to_vec()
            .unwrap();

        solana_instruction::Instruction {
            program_id: crate::TEMPO_PROGRAM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct AcceptAuthorityTransferInstructionData {
    discriminator: u8,
}

impl AcceptAuthorityTransferInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 37 }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for AcceptAuthorityTransferInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `AcceptAuthorityTransfer`.
///
/// ### Accounts:
///
///   0. `[signer]` new_authority
///   1. `[writable]` market
///   2. `[]` event_authority
///   3. `[]` tempo_program
#[derive(Clone, Debug, Default)]
pub struct AcceptAuthorityTransferBuilder {
    new_authority: Option<solana_address::Address>,
    market: Option<solana_address::Address>,
    event_authority: Option<solana_address::Address>,
    tempo_program: Option<solana_address::Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl AcceptAuthorityTransferBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The staged new authority
    #[inline(always)]
    pub fn new_authority(&mut self, new_authority: solana_address::Address) -> &mut Self {
        self.new_authority = Some(new_authority);
        self
    }
    /// Market whose authority rotates
    #[inline(always)]
    pub fn market(&mut self, market: solana_address::Address) -> &mut Self {
        self.market = Some(market);
        self
    }
    /// Event authority PDA for CPI event emission
    #[inline(always)]
    pub fn event_authority(&mut self, event_authority: solana_address::Address) -> &mut Self {
        self.event_authority = Some(event_authority);
        self
    }
    /// Tempo program, for self-CPI event emission
    #[inline(always)]
    pub fn tempo_program(&mut self, tempo_program: solana_address::Address) -> &mut Self {
        self.tempo_program = Some(tempo_program);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = AcceptAuthorityTransfer {
            new_authority: self.new_authority.expect("new_authority is not set"),
            market: self.market.expect("market is not set"),
            event_authority: self.event_authority.expect("event_authority is not set"),
            tempo_program: self.tempo_program.expect("tempo_program is not set"),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `accept_authority_transfer` CPI accounts.
pub struct AcceptAuthorityTransferCpiAccounts<'a, 'b> {
    /// The staged new authority
    pub new_authority: &'b solana_account_info::AccountInfo<'a>,
    /// Market whose authority rotates
    pub market: &'b solana_account_info::AccountInfo<'a>,
    /// Event authority PDA for CPI event emission
    pub event_authority: &'b solana_account_info::AccountInfo<'a>,
    /// Tempo program, for self-CPI event emission
    pub tempo_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `accept_authority_transfer` CPI instruction.
pub struct AcceptAuthorityTransferCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// The staged new authority
    pub new_authority: &'b solana_account_info::AccountInfo<'a>,
    /// Market whose authority rotates
    pub market: &'b solana_account_info::AccountInfo<'a>,
    /// Event authority PDA for CPI event emission
    pub event_authority: &'b solana_account_info::AccountInfo<'a>,
    /// Tempo program, for self-CPI event emission
    pub tempo_program: &'b solana_account_info::AccountInfo<'a>,
}

impl<'a, 'b> AcceptAuthorityTransferCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: AcceptAuthorityTransferCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            new_authority: accounts.new_authority,
            market: accounts.market,
            event_authority: accounts.event_authority,
            tempo_program: accounts.tempo_program,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.new_authority.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.market.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.event_authority.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.tempo_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let data = AcceptAuthorityTransferInstructionData::new()
            .try_to_vec()
            .unwrap();

        let instruction = solana_instruction::Instruction {
            program_id: crate::TEMPO_PROGRAM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(5 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.new_authority.clone());
        account_infos.push(self.market.clone());
        account_infos.push(self.event_authority.clone());
        account_infos.push(self.tempo_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `AcceptAuthorityTransfer` via CPI.
///
/// ### Accounts:
///
///   0. `[signer]` new_authority
///   1. `[writable]` market
///   2. `[]` event_authority
///   3. `[]` tempo_program
#[derive(Clone, Debug)]
pub struct AcceptAuthorityTransferCpiBuilder<'a, 'b> {
    instruction: Box<AcceptAuthorityTransferCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> AcceptAuthorityTransferCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(AcceptAuthorityTransferCpiBuilderInstruction {
            __program: program,
            new_authority: None,
            market: None,
            event_authority: None,
            tempo_program: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The staged new authority
    #[inline(always)]
    pub fn new_authority(
        &mut self,
        new_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.new_authority = Some(new_authority);
        self
    }
    /// Market whose authority rotates
    #[inline(always)]
    pub fn market(&mut self, market: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.market = Some(market);
        self
    }
    /// Event authority PDA for CPI event emission
    #[inline(always)]
    pub fn event_authority(
        &mut self,
        event_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.event_authority = Some(event_authority);
        self
    }
    /// Tempo program, for self-CPI event emission
    #[inline(always)]
    pub fn tempo_program(
        &mut self,
        tempo_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.tempo_program = Some(tempo_program);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = AcceptAuthorityTransferCpi {
            __program: self.instruction.__program,

            new_authority: self
                .instruction
                .new_authority
                .expect("new_authority is not set"),

            market: self.instruction.market.expect("market is not set"),

            event_authority: self
                .instruction
                .event_authority
                .expect("event_authority is not set"),

            tempo_program: self
                .instruction
                .tempo_program
                .expect("tempo_program is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct AcceptAuthorityTransferCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    new_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    market: Option<&'b solana_account_info::AccountInfo<'a>>,
    event_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    tempo_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}

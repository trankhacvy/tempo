/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const CLEAR_MAKER_QUOTE_DISCRIMINATOR = 19;

export function getClearMakerQuoteDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(CLEAR_MAKER_QUOTE_DISCRIMINATOR);
}

export type ClearMakerQuoteInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountWriter extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountMakerQuote extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountWriter extends string
        ? ReadonlySignerAccount<TAccountWriter> &
            AccountSignerMeta<TAccountWriter>
        : TAccountWriter,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountMakerQuote extends string
        ? WritableAccount<TAccountMakerQuote>
        : TAccountMakerQuote,
      ...TRemainingAccounts,
    ]
  >;

export type ClearMakerQuoteInstructionData = {
  discriminator: number;
  sequence: bigint;
};

export type ClearMakerQuoteInstructionDataArgs = { sequence: number | bigint };

export function getClearMakerQuoteInstructionDataEncoder(): FixedSizeEncoder<ClearMakerQuoteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["sequence", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CLEAR_MAKER_QUOTE_DISCRIMINATOR }),
  );
}

export function getClearMakerQuoteInstructionDataDecoder(): FixedSizeDecoder<ClearMakerQuoteInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["sequence", getU64Decoder()],
  ]);
}

export function getClearMakerQuoteInstructionDataCodec(): FixedSizeCodec<
  ClearMakerQuoteInstructionDataArgs,
  ClearMakerQuoteInstructionData
> {
  return combineCodec(
    getClearMakerQuoteInstructionDataEncoder(),
    getClearMakerQuoteInstructionDataDecoder(),
  );
}

export type ClearMakerQuoteInput<
  TAccountWriter extends string = string,
  TAccountMarket extends string = string,
  TAccountMakerQuote extends string = string,
> = {
  /** Maker or its delegate */
  writer: TransactionSigner<TAccountWriter>;
  /** Market (decrements active count) */
  market: Address<TAccountMarket>;
  /** MakerQuote to clear */
  makerQuote: Address<TAccountMakerQuote>;
  sequence: ClearMakerQuoteInstructionDataArgs["sequence"];
};

export function getClearMakerQuoteInstruction<
  TAccountWriter extends string,
  TAccountMarket extends string,
  TAccountMakerQuote extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ClearMakerQuoteInput<
    TAccountWriter,
    TAccountMarket,
    TAccountMakerQuote
  >,
  config?: { programAddress?: TProgramAddress },
): ClearMakerQuoteInstruction<
  TProgramAddress,
  TAccountWriter,
  TAccountMarket,
  TAccountMakerQuote
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    writer: { value: input.writer ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    makerQuote: { value: input.makerQuote ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("writer", accounts.writer),
      getAccountMeta("market", accounts.market),
      getAccountMeta("makerQuote", accounts.makerQuote),
    ],
    data: getClearMakerQuoteInstructionDataEncoder().encode(
      args as ClearMakerQuoteInstructionDataArgs,
    ),
    programAddress,
  } as ClearMakerQuoteInstruction<
    TProgramAddress,
    TAccountWriter,
    TAccountMarket,
    TAccountMakerQuote
  >);
}

export type ParsedClearMakerQuoteInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Maker or its delegate */
    writer: TAccountMetas[0];
    /** Market (decrements active count) */
    market: TAccountMetas[1];
    /** MakerQuote to clear */
    makerQuote: TAccountMetas[2];
  };
  data: ClearMakerQuoteInstructionData;
};

export function parseClearMakerQuoteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClearMakerQuoteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      writer: getNextAccount(),
      market: getNextAccount(),
      makerQuote: getNextAccount(),
    },
    data: getClearMakerQuoteInstructionDataDecoder().decode(instruction.data),
  };
}

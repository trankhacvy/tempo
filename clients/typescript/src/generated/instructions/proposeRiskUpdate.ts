/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const PROPOSE_RISK_UPDATE_DISCRIMINATOR = 34;

export function getProposeRiskUpdateDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(PROPOSE_RISK_UPDATE_DISCRIMINATOR);
}

export type ProposeRiskUpdateInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeRiskUpdateInstructionData = {
  discriminator: number;
  maintenanceMarginBps: number;
  initialMarginBps: number;
  liquidationPenaltyBps: number;
  liquidationCloseBufferBps: number;
};

export type ProposeRiskUpdateInstructionDataArgs = {
  maintenanceMarginBps: number;
  initialMarginBps: number;
  liquidationPenaltyBps: number;
  liquidationCloseBufferBps: number;
};

export function getProposeRiskUpdateInstructionDataEncoder(): FixedSizeEncoder<ProposeRiskUpdateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["maintenanceMarginBps", getU16Encoder()],
      ["initialMarginBps", getU16Encoder()],
      ["liquidationPenaltyBps", getU16Encoder()],
      ["liquidationCloseBufferBps", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PROPOSE_RISK_UPDATE_DISCRIMINATOR }),
  );
}

export function getProposeRiskUpdateInstructionDataDecoder(): FixedSizeDecoder<ProposeRiskUpdateInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["maintenanceMarginBps", getU16Decoder()],
    ["initialMarginBps", getU16Decoder()],
    ["liquidationPenaltyBps", getU16Decoder()],
    ["liquidationCloseBufferBps", getU16Decoder()],
  ]);
}

export function getProposeRiskUpdateInstructionDataCodec(): FixedSizeCodec<
  ProposeRiskUpdateInstructionDataArgs,
  ProposeRiskUpdateInstructionData
> {
  return combineCodec(
    getProposeRiskUpdateInstructionDataEncoder(),
    getProposeRiskUpdateInstructionDataDecoder(),
  );
}

export type ProposeRiskUpdateInput<
  TAccountAuthority extends string = string,
  TAccountMarket extends string = string,
> = {
  /** Market authority */
  authority: TransactionSigner<TAccountAuthority>;
  /** Market (staging slot written) */
  market: Address<TAccountMarket>;
  maintenanceMarginBps: ProposeRiskUpdateInstructionDataArgs["maintenanceMarginBps"];
  initialMarginBps: ProposeRiskUpdateInstructionDataArgs["initialMarginBps"];
  liquidationPenaltyBps: ProposeRiskUpdateInstructionDataArgs["liquidationPenaltyBps"];
  liquidationCloseBufferBps: ProposeRiskUpdateInstructionDataArgs["liquidationCloseBufferBps"];
};

export function getProposeRiskUpdateInstruction<
  TAccountAuthority extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ProposeRiskUpdateInput<TAccountAuthority, TAccountMarket>,
  config?: { programAddress?: TProgramAddress },
): ProposeRiskUpdateInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountMarket
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("market", accounts.market),
    ],
    data: getProposeRiskUpdateInstructionDataEncoder().encode(
      args as ProposeRiskUpdateInstructionDataArgs,
    ),
    programAddress,
  } as ProposeRiskUpdateInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountMarket
  >);
}

export type ParsedProposeRiskUpdateInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Market authority */
    authority: TAccountMetas[0];
    /** Market (staging slot written) */
    market: TAccountMetas[1];
  };
  data: ProposeRiskUpdateInstructionData;
};

export function parseProposeRiskUpdateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposeRiskUpdateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), market: getNextAccount() },
    data: getProposeRiskUpdateInstructionDataDecoder().decode(instruction.data),
  };
}

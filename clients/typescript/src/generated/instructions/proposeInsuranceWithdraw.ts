/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const PROPOSE_INSURANCE_WITHDRAW_DISCRIMINATOR = 41;

export function getProposeInsuranceWithdrawDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(PROPOSE_INSURANCE_WITHDRAW_DISCRIMINATOR);
}

export type ProposeInsuranceWithdrawInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeInsuranceWithdrawInstructionData = {
  discriminator: number;
  amount: bigint;
};

export type ProposeInsuranceWithdrawInstructionDataArgs = {
  amount: number | bigint;
};

export function getProposeInsuranceWithdrawInstructionDataEncoder(): FixedSizeEncoder<ProposeInsuranceWithdrawInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PROPOSE_INSURANCE_WITHDRAW_DISCRIMINATOR,
    }),
  );
}

export function getProposeInsuranceWithdrawInstructionDataDecoder(): FixedSizeDecoder<ProposeInsuranceWithdrawInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["amount", getU64Decoder()],
  ]);
}

export function getProposeInsuranceWithdrawInstructionDataCodec(): FixedSizeCodec<
  ProposeInsuranceWithdrawInstructionDataArgs,
  ProposeInsuranceWithdrawInstructionData
> {
  return combineCodec(
    getProposeInsuranceWithdrawInstructionDataEncoder(),
    getProposeInsuranceWithdrawInstructionDataDecoder(),
  );
}

export type ProposeInsuranceWithdrawInput<
  TAccountAuthority extends string = string,
  TAccountVault extends string = string,
> = {
  /** Vault authority (recorded at init) */
  authority: TransactionSigner<TAccountAuthority>;
  /** Vault (staging slot written) */
  vault: Address<TAccountVault>;
  amount: ProposeInsuranceWithdrawInstructionDataArgs["amount"];
};

export function getProposeInsuranceWithdrawInstruction<
  TAccountAuthority extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ProposeInsuranceWithdrawInput<TAccountAuthority, TAccountVault>,
  config?: { programAddress?: TProgramAddress },
): ProposeInsuranceWithdrawInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountVault
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("vault", accounts.vault),
    ],
    data: getProposeInsuranceWithdrawInstructionDataEncoder().encode(
      args as ProposeInsuranceWithdrawInstructionDataArgs,
    ),
    programAddress,
  } as ProposeInsuranceWithdrawInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountVault
  >);
}

export type ParsedProposeInsuranceWithdrawInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Vault authority (recorded at init) */
    authority: TAccountMetas[0];
    /** Vault (staging slot written) */
    vault: TAccountMetas[1];
  };
  data: ProposeInsuranceWithdrawInstructionData;
};

export function parseProposeInsuranceWithdrawInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposeInsuranceWithdrawInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), vault: getNextAccount() },
    data: getProposeInsuranceWithdrawInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}

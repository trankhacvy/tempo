/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findOrderSlabHeaderPda } from "../pdas";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const CANCEL_ALL_ORDERS_DISCRIMINATOR = 43;

export function getCancelAllOrdersDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(CANCEL_ALL_ORDERS_DISCRIMINATOR);
}

export type CancelAllOrdersInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountTrader extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountOrderSlab extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountTempoProgram extends string | AccountMeta<string> = string,
  TAccountUserCollateral extends string | AccountMeta<string> | undefined =
    undefined,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTrader extends string
        ? ReadonlySignerAccount<TAccountTrader> &
            AccountSignerMeta<TAccountTrader>
        : TAccountTrader,
      TAccountMarket extends string
        ? ReadonlyAccount<TAccountMarket>
        : TAccountMarket,
      TAccountOrderSlab extends string
        ? WritableAccount<TAccountOrderSlab>
        : TAccountOrderSlab,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountTempoProgram extends string
        ? ReadonlyAccount<TAccountTempoProgram>
        : TAccountTempoProgram,
      ...(TAccountUserCollateral extends undefined
        ? []
        : [
            TAccountUserCollateral extends string
              ? WritableAccount<TAccountUserCollateral>
              : TAccountUserCollateral,
          ]),
      ...TRemainingAccounts,
    ]
  >;

export type CancelAllOrdersInstructionData = { discriminator: number };

export type CancelAllOrdersInstructionDataArgs = {};

export function getCancelAllOrdersInstructionDataEncoder(): FixedSizeEncoder<CancelAllOrdersInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: CANCEL_ALL_ORDERS_DISCRIMINATOR }),
  );
}

export function getCancelAllOrdersInstructionDataDecoder(): FixedSizeDecoder<CancelAllOrdersInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getCancelAllOrdersInstructionDataCodec(): FixedSizeCodec<
  CancelAllOrdersInstructionDataArgs,
  CancelAllOrdersInstructionData
> {
  return combineCodec(
    getCancelAllOrdersInstructionDataEncoder(),
    getCancelAllOrdersInstructionDataDecoder(),
  );
}

export type CancelAllOrdersAsyncInput<
  TAccountTrader extends string = string,
  TAccountMarket extends string = string,
  TAccountOrderSlab extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountTempoProgram extends string = string,
  TAccountUserCollateral extends string = string,
> = {
  /** Order owner (only their orders cancel) */
  trader: TransactionSigner<TAccountTrader>;
  /** Market the orders belong to. Read-only (Design Z): cancel writes only its own shard. */
  market: Address<TAccountMarket>;
  /** The one OrderSlab shard to scan (multi-shard = one tx per shard) */
  orderSlab?: Address<TAccountOrderSlab>;
  /** Event authority PDA for CPI event emission */
  eventAuthority: Address<TAccountEventAuthority>;
  /** Tempo program, for self-CPI event emission */
  tempoProgram: Address<TAccountTempoProgram>;
  /** (Optional) trader's collateral ledger; releases the summed reserved margin */
  userCollateral?: Address<TAccountUserCollateral>;
};

export async function getCancelAllOrdersInstructionAsync<
  TAccountTrader extends string,
  TAccountMarket extends string,
  TAccountOrderSlab extends string,
  TAccountEventAuthority extends string,
  TAccountTempoProgram extends string,
  TAccountUserCollateral extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: CancelAllOrdersAsyncInput<
    TAccountTrader,
    TAccountMarket,
    TAccountOrderSlab,
    TAccountEventAuthority,
    TAccountTempoProgram,
    TAccountUserCollateral
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CancelAllOrdersInstruction<
    TProgramAddress,
    TAccountTrader,
    TAccountMarket,
    TAccountOrderSlab,
    TAccountEventAuthority,
    TAccountTempoProgram,
    TAccountUserCollateral
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    trader: { value: input.trader ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: false },
    orderSlab: { value: input.orderSlab ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    tempoProgram: { value: input.tempoProgram ?? null, isWritable: false },
    userCollateral: { value: input.userCollateral ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.orderSlab.value) {
    accounts.orderSlab.value = await findOrderSlabHeaderPda({
      market: getAddressFromResolvedInstructionAccount(
        "market",
        accounts.market.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("trader", accounts.trader),
      getAccountMeta("market", accounts.market),
      getAccountMeta("orderSlab", accounts.orderSlab),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("tempoProgram", accounts.tempoProgram),
      getAccountMeta("userCollateral", accounts.userCollateral),
    ].filter(<T>(x: T | undefined): x is T => x !== undefined),
    data: getCancelAllOrdersInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelAllOrdersInstruction<
    TProgramAddress,
    TAccountTrader,
    TAccountMarket,
    TAccountOrderSlab,
    TAccountEventAuthority,
    TAccountTempoProgram,
    TAccountUserCollateral
  >);
}

export type CancelAllOrdersInput<
  TAccountTrader extends string = string,
  TAccountMarket extends string = string,
  TAccountOrderSlab extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountTempoProgram extends string = string,
  TAccountUserCollateral extends string = string,
> = {
  /** Order owner (only their orders cancel) */
  trader: TransactionSigner<TAccountTrader>;
  /** Market the orders belong to. Read-only (Design Z): cancel writes only its own shard. */
  market: Address<TAccountMarket>;
  /** The one OrderSlab shard to scan (multi-shard = one tx per shard) */
  orderSlab: Address<TAccountOrderSlab>;
  /** Event authority PDA for CPI event emission */
  eventAuthority: Address<TAccountEventAuthority>;
  /** Tempo program, for self-CPI event emission */
  tempoProgram: Address<TAccountTempoProgram>;
  /** (Optional) trader's collateral ledger; releases the summed reserved margin */
  userCollateral?: Address<TAccountUserCollateral>;
};

export function getCancelAllOrdersInstruction<
  TAccountTrader extends string,
  TAccountMarket extends string,
  TAccountOrderSlab extends string,
  TAccountEventAuthority extends string,
  TAccountTempoProgram extends string,
  TAccountUserCollateral extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: CancelAllOrdersInput<
    TAccountTrader,
    TAccountMarket,
    TAccountOrderSlab,
    TAccountEventAuthority,
    TAccountTempoProgram,
    TAccountUserCollateral
  >,
  config?: { programAddress?: TProgramAddress },
): CancelAllOrdersInstruction<
  TProgramAddress,
  TAccountTrader,
  TAccountMarket,
  TAccountOrderSlab,
  TAccountEventAuthority,
  TAccountTempoProgram,
  TAccountUserCollateral
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    trader: { value: input.trader ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: false },
    orderSlab: { value: input.orderSlab ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    tempoProgram: { value: input.tempoProgram ?? null, isWritable: false },
    userCollateral: { value: input.userCollateral ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("trader", accounts.trader),
      getAccountMeta("market", accounts.market),
      getAccountMeta("orderSlab", accounts.orderSlab),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("tempoProgram", accounts.tempoProgram),
      getAccountMeta("userCollateral", accounts.userCollateral),
    ].filter(<T>(x: T | undefined): x is T => x !== undefined),
    data: getCancelAllOrdersInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelAllOrdersInstruction<
    TProgramAddress,
    TAccountTrader,
    TAccountMarket,
    TAccountOrderSlab,
    TAccountEventAuthority,
    TAccountTempoProgram,
    TAccountUserCollateral
  >);
}

export type ParsedCancelAllOrdersInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Order owner (only their orders cancel) */
    trader: TAccountMetas[0];
    /** Market the orders belong to. Read-only (Design Z): cancel writes only its own shard. */
    market: TAccountMetas[1];
    /** The one OrderSlab shard to scan (multi-shard = one tx per shard) */
    orderSlab: TAccountMetas[2];
    /** Event authority PDA for CPI event emission */
    eventAuthority: TAccountMetas[3];
    /** Tempo program, for self-CPI event emission */
    tempoProgram: TAccountMetas[4];
    /** (Optional) trader's collateral ledger; releases the summed reserved margin */
    userCollateral?: TAccountMetas[5] | undefined;
  };
  data: CancelAllOrdersInstructionData;
};

export function parseCancelAllOrdersInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelAllOrdersInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  let optionalAccountsRemaining = instruction.accounts.length - 5;
  const getNextOptionalAccount = () => {
    if (optionalAccountsRemaining === 0) return undefined;
    optionalAccountsRemaining -= 1;
    return getNextAccount();
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      trader: getNextAccount(),
      market: getNextAccount(),
      orderSlab: getNextAccount(),
      eventAuthority: getNextAccount(),
      tempoProgram: getNextAccount(),
      userCollateral: getNextOptionalAccount(),
    },
    data: getCancelAllOrdersInstructionDataDecoder().decode(instruction.data),
  };
}

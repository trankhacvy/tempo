/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getI16Decoder,
  getI16Encoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ProgramDerivedAddress,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getResolvedInstructionAccountAsProgramDerivedAddress,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findAuctionHistogramHeaderPda,
  findMarketPda,
  findOrderSlabHeaderPda,
} from "../pdas";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_MARKET_DISCRIMINATOR = 0;

export function getInitializeMarketDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(INITIALIZE_MARKET_DISCRIMINATOR);
}

export type InitializeMarketInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountMarketSeed extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountHistogram extends string | AccountMeta<string> = string,
  TAccountOrderSlab extends string | AccountMeta<string> = string,
  TAccountOracle extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountTempoProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountMarketSeed extends string
        ? ReadonlySignerAccount<TAccountMarketSeed> &
            AccountSignerMeta<TAccountMarketSeed>
        : TAccountMarketSeed,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountHistogram extends string
        ? WritableAccount<TAccountHistogram>
        : TAccountHistogram,
      TAccountOrderSlab extends string
        ? WritableAccount<TAccountOrderSlab>
        : TAccountOrderSlab,
      TAccountOracle extends string
        ? ReadonlyAccount<TAccountOracle>
        : TAccountOracle,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountTempoProgram extends string
        ? ReadonlyAccount<TAccountTempoProgram>
        : TAccountTempoProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeMarketInstructionData = {
  discriminator: number;
  marketBump: number;
  histogramBump: number;
  orderSlabBump: number;
  tickSize: bigint;
  numTicks: number;
  ordersPerAuctionCap: number;
  oracleFeedId: Array<number>;
  maintenanceMarginBps: number;
  liquidationPenaltyBps: number;
  makerFeeBps: number;
  takerFeeBps: number;
  integratorShareBps: number;
  crankFee: bigint;
  collateralMint: Array<number>;
  maxPriceMoveBpsPerSlot: number;
  softStaleSlots: bigint;
  initialMarginBps: number;
  maxPositionNotional: bigint;
};

export type InitializeMarketInstructionDataArgs = {
  marketBump: number;
  histogramBump: number;
  orderSlabBump: number;
  tickSize: number | bigint;
  numTicks: number;
  ordersPerAuctionCap: number;
  oracleFeedId: Array<number>;
  maintenanceMarginBps: number;
  liquidationPenaltyBps: number;
  makerFeeBps: number;
  takerFeeBps: number;
  integratorShareBps: number;
  crankFee: number | bigint;
  collateralMint: Array<number>;
  maxPriceMoveBpsPerSlot: number;
  softStaleSlots: number | bigint;
  initialMarginBps: number;
  maxPositionNotional: number | bigint;
};

export function getInitializeMarketInstructionDataEncoder(): FixedSizeEncoder<InitializeMarketInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["marketBump", getU8Encoder()],
      ["histogramBump", getU8Encoder()],
      ["orderSlabBump", getU8Encoder()],
      ["tickSize", getU64Encoder()],
      ["numTicks", getU32Encoder()],
      ["ordersPerAuctionCap", getU32Encoder()],
      ["oracleFeedId", getArrayEncoder(getU8Encoder(), { size: 32 })],
      ["maintenanceMarginBps", getU16Encoder()],
      ["liquidationPenaltyBps", getU16Encoder()],
      ["makerFeeBps", getI16Encoder()],
      ["takerFeeBps", getI16Encoder()],
      ["integratorShareBps", getU16Encoder()],
      ["crankFee", getU64Encoder()],
      ["collateralMint", getArrayEncoder(getU8Encoder(), { size: 32 })],
      ["maxPriceMoveBpsPerSlot", getU16Encoder()],
      ["softStaleSlots", getU64Encoder()],
      ["initialMarginBps", getU16Encoder()],
      ["maxPositionNotional", getU128Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_MARKET_DISCRIMINATOR }),
  );
}

export function getInitializeMarketInstructionDataDecoder(): FixedSizeDecoder<InitializeMarketInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["marketBump", getU8Decoder()],
    ["histogramBump", getU8Decoder()],
    ["orderSlabBump", getU8Decoder()],
    ["tickSize", getU64Decoder()],
    ["numTicks", getU32Decoder()],
    ["ordersPerAuctionCap", getU32Decoder()],
    ["oracleFeedId", getArrayDecoder(getU8Decoder(), { size: 32 })],
    ["maintenanceMarginBps", getU16Decoder()],
    ["liquidationPenaltyBps", getU16Decoder()],
    ["makerFeeBps", getI16Decoder()],
    ["takerFeeBps", getI16Decoder()],
    ["integratorShareBps", getU16Decoder()],
    ["crankFee", getU64Decoder()],
    ["collateralMint", getArrayDecoder(getU8Decoder(), { size: 32 })],
    ["maxPriceMoveBpsPerSlot", getU16Decoder()],
    ["softStaleSlots", getU64Decoder()],
    ["initialMarginBps", getU16Decoder()],
    ["maxPositionNotional", getU128Decoder()],
  ]);
}

export function getInitializeMarketInstructionDataCodec(): FixedSizeCodec<
  InitializeMarketInstructionDataArgs,
  InitializeMarketInstructionData
> {
  return combineCodec(
    getInitializeMarketInstructionDataEncoder(),
    getInitializeMarketInstructionDataDecoder(),
  );
}

export type InitializeMarketAsyncInput<
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountMarketSeed extends string = string,
  TAccountMarket extends string = string,
  TAccountHistogram extends string = string,
  TAccountOrderSlab extends string = string,
  TAccountOracle extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountTempoProgram extends string = string,
> = {
  /** Pays for account creation */
  payer: TransactionSigner<TAccountPayer>;
  /** Market authority / admin */
  authority: TransactionSigner<TAccountAuthority>;
  /** Random keypair seed for the market PDA */
  marketSeed: TransactionSigner<TAccountMarketSeed>;
  /** Market PDA to be created */
  market?: ProgramDerivedAddress<TAccountMarket>;
  /** AuctionHistogram PDA (the mailboxes) to be created */
  histogram?: ProgramDerivedAddress<TAccountHistogram>;
  /** OrderSlab PDA (bounded resting-order slots) to be created */
  orderSlab?: ProgramDerivedAddress<TAccountOrderSlab>;
  /** Oracle (Pyth PriceUpdateV2) recorded on the market; consumed by funding/liquidation */
  oracle: Address<TAccountOracle>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
  /** Event authority PDA for CPI event emission */
  eventAuthority: Address<TAccountEventAuthority>;
  /** Tempo program, for self-CPI event emission */
  tempoProgram: Address<TAccountTempoProgram>;
  marketBump?: InitializeMarketInstructionDataArgs["marketBump"];
  histogramBump?: InitializeMarketInstructionDataArgs["histogramBump"];
  orderSlabBump?: InitializeMarketInstructionDataArgs["orderSlabBump"];
  tickSize: InitializeMarketInstructionDataArgs["tickSize"];
  numTicks: InitializeMarketInstructionDataArgs["numTicks"];
  ordersPerAuctionCap: InitializeMarketInstructionDataArgs["ordersPerAuctionCap"];
  oracleFeedId: InitializeMarketInstructionDataArgs["oracleFeedId"];
  maintenanceMarginBps: InitializeMarketInstructionDataArgs["maintenanceMarginBps"];
  liquidationPenaltyBps: InitializeMarketInstructionDataArgs["liquidationPenaltyBps"];
  makerFeeBps: InitializeMarketInstructionDataArgs["makerFeeBps"];
  takerFeeBps: InitializeMarketInstructionDataArgs["takerFeeBps"];
  integratorShareBps: InitializeMarketInstructionDataArgs["integratorShareBps"];
  crankFee: InitializeMarketInstructionDataArgs["crankFee"];
  collateralMint: InitializeMarketInstructionDataArgs["collateralMint"];
  maxPriceMoveBpsPerSlot: InitializeMarketInstructionDataArgs["maxPriceMoveBpsPerSlot"];
  softStaleSlots: InitializeMarketInstructionDataArgs["softStaleSlots"];
  initialMarginBps: InitializeMarketInstructionDataArgs["initialMarginBps"];
  maxPositionNotional: InitializeMarketInstructionDataArgs["maxPositionNotional"];
};

export async function getInitializeMarketInstructionAsync<
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TAccountMarketSeed extends string,
  TAccountMarket extends string,
  TAccountHistogram extends string,
  TAccountOrderSlab extends string,
  TAccountOracle extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountTempoProgram extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: InitializeMarketAsyncInput<
    TAccountPayer,
    TAccountAuthority,
    TAccountMarketSeed,
    TAccountMarket,
    TAccountHistogram,
    TAccountOrderSlab,
    TAccountOracle,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountTempoProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeMarketInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountMarketSeed,
    TAccountMarket,
    TAccountHistogram,
    TAccountOrderSlab,
    TAccountOracle,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountTempoProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    marketSeed: { value: input.marketSeed ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    histogram: { value: input.histogram ?? null, isWritable: true },
    orderSlab: { value: input.orderSlab ?? null, isWritable: true },
    oracle: { value: input.oracle ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    tempoProgram: { value: input.tempoProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.market.value) {
    accounts.market.value = await findMarketPda({
      marketSeed: getAddressFromResolvedInstructionAccount(
        "marketSeed",
        accounts.marketSeed.value,
      ),
    });
  }
  if (!accounts.histogram.value) {
    accounts.histogram.value = await findAuctionHistogramHeaderPda({
      market: getAddressFromResolvedInstructionAccount(
        "market",
        accounts.market.value,
      ),
    });
  }
  if (!accounts.orderSlab.value) {
    accounts.orderSlab.value = await findOrderSlabHeaderPda({
      market: getAddressFromResolvedInstructionAccount(
        "market",
        accounts.market.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!args.marketBump) {
    args.marketBump = getResolvedInstructionAccountAsProgramDerivedAddress(
      "market",
      accounts.market.value,
    )[1];
  }
  if (!args.histogramBump) {
    args.histogramBump = getResolvedInstructionAccountAsProgramDerivedAddress(
      "histogram",
      accounts.histogram.value,
    )[1];
  }
  if (!args.orderSlabBump) {
    args.orderSlabBump = getResolvedInstructionAccountAsProgramDerivedAddress(
      "orderSlab",
      accounts.orderSlab.value,
    )[1];
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("marketSeed", accounts.marketSeed),
      getAccountMeta("market", accounts.market),
      getAccountMeta("histogram", accounts.histogram),
      getAccountMeta("orderSlab", accounts.orderSlab),
      getAccountMeta("oracle", accounts.oracle),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("tempoProgram", accounts.tempoProgram),
    ],
    data: getInitializeMarketInstructionDataEncoder().encode(
      args as InitializeMarketInstructionDataArgs,
    ),
    programAddress,
  } as InitializeMarketInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountMarketSeed,
    TAccountMarket,
    TAccountHistogram,
    TAccountOrderSlab,
    TAccountOracle,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountTempoProgram
  >);
}

export type InitializeMarketInput<
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountMarketSeed extends string = string,
  TAccountMarket extends string = string,
  TAccountHistogram extends string = string,
  TAccountOrderSlab extends string = string,
  TAccountOracle extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountTempoProgram extends string = string,
> = {
  /** Pays for account creation */
  payer: TransactionSigner<TAccountPayer>;
  /** Market authority / admin */
  authority: TransactionSigner<TAccountAuthority>;
  /** Random keypair seed for the market PDA */
  marketSeed: TransactionSigner<TAccountMarketSeed>;
  /** Market PDA to be created */
  market: ProgramDerivedAddress<TAccountMarket>;
  /** AuctionHistogram PDA (the mailboxes) to be created */
  histogram: ProgramDerivedAddress<TAccountHistogram>;
  /** OrderSlab PDA (bounded resting-order slots) to be created */
  orderSlab: ProgramDerivedAddress<TAccountOrderSlab>;
  /** Oracle (Pyth PriceUpdateV2) recorded on the market; consumed by funding/liquidation */
  oracle: Address<TAccountOracle>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
  /** Event authority PDA for CPI event emission */
  eventAuthority: Address<TAccountEventAuthority>;
  /** Tempo program, for self-CPI event emission */
  tempoProgram: Address<TAccountTempoProgram>;
  marketBump?: InitializeMarketInstructionDataArgs["marketBump"];
  histogramBump?: InitializeMarketInstructionDataArgs["histogramBump"];
  orderSlabBump?: InitializeMarketInstructionDataArgs["orderSlabBump"];
  tickSize: InitializeMarketInstructionDataArgs["tickSize"];
  numTicks: InitializeMarketInstructionDataArgs["numTicks"];
  ordersPerAuctionCap: InitializeMarketInstructionDataArgs["ordersPerAuctionCap"];
  oracleFeedId: InitializeMarketInstructionDataArgs["oracleFeedId"];
  maintenanceMarginBps: InitializeMarketInstructionDataArgs["maintenanceMarginBps"];
  liquidationPenaltyBps: InitializeMarketInstructionDataArgs["liquidationPenaltyBps"];
  makerFeeBps: InitializeMarketInstructionDataArgs["makerFeeBps"];
  takerFeeBps: InitializeMarketInstructionDataArgs["takerFeeBps"];
  integratorShareBps: InitializeMarketInstructionDataArgs["integratorShareBps"];
  crankFee: InitializeMarketInstructionDataArgs["crankFee"];
  collateralMint: InitializeMarketInstructionDataArgs["collateralMint"];
  maxPriceMoveBpsPerSlot: InitializeMarketInstructionDataArgs["maxPriceMoveBpsPerSlot"];
  softStaleSlots: InitializeMarketInstructionDataArgs["softStaleSlots"];
  initialMarginBps: InitializeMarketInstructionDataArgs["initialMarginBps"];
  maxPositionNotional: InitializeMarketInstructionDataArgs["maxPositionNotional"];
};

export function getInitializeMarketInstruction<
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TAccountMarketSeed extends string,
  TAccountMarket extends string,
  TAccountHistogram extends string,
  TAccountOrderSlab extends string,
  TAccountOracle extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountTempoProgram extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: InitializeMarketInput<
    TAccountPayer,
    TAccountAuthority,
    TAccountMarketSeed,
    TAccountMarket,
    TAccountHistogram,
    TAccountOrderSlab,
    TAccountOracle,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountTempoProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeMarketInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountAuthority,
  TAccountMarketSeed,
  TAccountMarket,
  TAccountHistogram,
  TAccountOrderSlab,
  TAccountOracle,
  TAccountSystemProgram,
  TAccountEventAuthority,
  TAccountTempoProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    marketSeed: { value: input.marketSeed ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    histogram: { value: input.histogram ?? null, isWritable: true },
    orderSlab: { value: input.orderSlab ?? null, isWritable: true },
    oracle: { value: input.oracle ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    tempoProgram: { value: input.tempoProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!args.marketBump) {
    args.marketBump = getResolvedInstructionAccountAsProgramDerivedAddress(
      "market",
      accounts.market.value,
    )[1];
  }
  if (!args.histogramBump) {
    args.histogramBump = getResolvedInstructionAccountAsProgramDerivedAddress(
      "histogram",
      accounts.histogram.value,
    )[1];
  }
  if (!args.orderSlabBump) {
    args.orderSlabBump = getResolvedInstructionAccountAsProgramDerivedAddress(
      "orderSlab",
      accounts.orderSlab.value,
    )[1];
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("marketSeed", accounts.marketSeed),
      getAccountMeta("market", accounts.market),
      getAccountMeta("histogram", accounts.histogram),
      getAccountMeta("orderSlab", accounts.orderSlab),
      getAccountMeta("oracle", accounts.oracle),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("tempoProgram", accounts.tempoProgram),
    ],
    data: getInitializeMarketInstructionDataEncoder().encode(
      args as InitializeMarketInstructionDataArgs,
    ),
    programAddress,
  } as InitializeMarketInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountMarketSeed,
    TAccountMarket,
    TAccountHistogram,
    TAccountOrderSlab,
    TAccountOracle,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountTempoProgram
  >);
}

export type ParsedInitializeMarketInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pays for account creation */
    payer: TAccountMetas[0];
    /** Market authority / admin */
    authority: TAccountMetas[1];
    /** Random keypair seed for the market PDA */
    marketSeed: TAccountMetas[2];
    /** Market PDA to be created */
    market: TAccountMetas[3];
    /** AuctionHistogram PDA (the mailboxes) to be created */
    histogram: TAccountMetas[4];
    /** OrderSlab PDA (bounded resting-order slots) to be created */
    orderSlab: TAccountMetas[5];
    /** Oracle (Pyth PriceUpdateV2) recorded on the market; consumed by funding/liquidation */
    oracle: TAccountMetas[6];
    /** System program */
    systemProgram: TAccountMetas[7];
    /** Event authority PDA for CPI event emission */
    eventAuthority: TAccountMetas[8];
    /** Tempo program, for self-CPI event emission */
    tempoProgram: TAccountMetas[9];
  };
  data: InitializeMarketInstructionData;
};

export function parseInitializeMarketInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeMarketInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 10) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 10,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      authority: getNextAccount(),
      marketSeed: getNextAccount(),
      market: getNextAccount(),
      histogram: getNextAccount(),
      orderSlab: getNextAccount(),
      oracle: getNextAccount(),
      systemProgram: getNextAccount(),
      eventAuthority: getNextAccount(),
      tempoProgram: getNextAccount(),
    },
    data: getInitializeMarketInstructionDataDecoder().decode(instruction.data),
  };
}

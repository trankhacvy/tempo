/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const APPLY_SET_ORACLE_DISCRIMINATOR = 39;

export function getApplySetOracleDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(APPLY_SET_ORACLE_DISCRIMINATOR);
}

export type ApplySetOracleInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountCranker extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountNewOracle extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountTempoProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCranker extends string
        ? ReadonlySignerAccount<TAccountCranker> &
            AccountSignerMeta<TAccountCranker>
        : TAccountCranker,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountNewOracle extends string
        ? ReadonlyAccount<TAccountNewOracle>
        : TAccountNewOracle,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountTempoProgram extends string
        ? ReadonlyAccount<TAccountTempoProgram>
        : TAccountTempoProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ApplySetOracleInstructionData = { discriminator: number };

export type ApplySetOracleInstructionDataArgs = {};

export function getApplySetOracleInstructionDataEncoder(): FixedSizeEncoder<ApplySetOracleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: APPLY_SET_ORACLE_DISCRIMINATOR }),
  );
}

export function getApplySetOracleInstructionDataDecoder(): FixedSizeDecoder<ApplySetOracleInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getApplySetOracleInstructionDataCodec(): FixedSizeCodec<
  ApplySetOracleInstructionDataArgs,
  ApplySetOracleInstructionData
> {
  return combineCodec(
    getApplySetOracleInstructionDataEncoder(),
    getApplySetOracleInstructionDataDecoder(),
  );
}

export type ApplySetOracleInput<
  TAccountCranker extends string = string,
  TAccountMarket extends string = string,
  TAccountNewOracle extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountTempoProgram extends string = string,
> = {
  /** Permissionless caller */
  cranker: TransactionSigner<TAccountCranker>;
  /** Market to repoint */
  market: Address<TAccountMarket>;
  /** The STAGED Pyth account (validated live before commit) */
  newOracle: Address<TAccountNewOracle>;
  /** Event authority PDA for CPI event emission */
  eventAuthority: Address<TAccountEventAuthority>;
  /** Tempo program, for self-CPI event emission */
  tempoProgram: Address<TAccountTempoProgram>;
};

export function getApplySetOracleInstruction<
  TAccountCranker extends string,
  TAccountMarket extends string,
  TAccountNewOracle extends string,
  TAccountEventAuthority extends string,
  TAccountTempoProgram extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ApplySetOracleInput<
    TAccountCranker,
    TAccountMarket,
    TAccountNewOracle,
    TAccountEventAuthority,
    TAccountTempoProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ApplySetOracleInstruction<
  TProgramAddress,
  TAccountCranker,
  TAccountMarket,
  TAccountNewOracle,
  TAccountEventAuthority,
  TAccountTempoProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    newOracle: { value: input.newOracle ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    tempoProgram: { value: input.tempoProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("market", accounts.market),
      getAccountMeta("newOracle", accounts.newOracle),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("tempoProgram", accounts.tempoProgram),
    ],
    data: getApplySetOracleInstructionDataEncoder().encode({}),
    programAddress,
  } as ApplySetOracleInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountMarket,
    TAccountNewOracle,
    TAccountEventAuthority,
    TAccountTempoProgram
  >);
}

export type ParsedApplySetOracleInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Permissionless caller */
    cranker: TAccountMetas[0];
    /** Market to repoint */
    market: TAccountMetas[1];
    /** The STAGED Pyth account (validated live before commit) */
    newOracle: TAccountMetas[2];
    /** Event authority PDA for CPI event emission */
    eventAuthority: TAccountMetas[3];
    /** Tempo program, for self-CPI event emission */
    tempoProgram: TAccountMetas[4];
  };
  data: ApplySetOracleInstructionData;
};

export function parseApplySetOracleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedApplySetOracleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      cranker: getNextAccount(),
      market: getNextAccount(),
      newOracle: getNextAccount(),
      eventAuthority: getNextAccount(),
      tempoProgram: getNextAccount(),
    },
    data: getApplySetOracleInstructionDataDecoder().decode(instruction.data),
  };
}

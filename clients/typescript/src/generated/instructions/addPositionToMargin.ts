/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findOrderSlabHeaderPda } from "../pdas";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const ADD_POSITION_TO_MARGIN_DISCRIMINATOR = 23;

export function getAddPositionToMarginDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(ADD_POSITION_TO_MARGIN_DISCRIMINATOR);
}

export type AddPositionToMarginInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountMarginAccount extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountOrderSlab extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountMarginAccount extends string
        ? WritableAccount<TAccountMarginAccount>
        : TAccountMarginAccount,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      TAccountMarket extends string
        ? ReadonlyAccount<TAccountMarket>
        : TAccountMarket,
      TAccountOrderSlab extends string
        ? ReadonlyAccount<TAccountOrderSlab>
        : TAccountOrderSlab,
      ...TRemainingAccounts,
    ]
  >;

export type AddPositionToMarginInstructionData = { discriminator: number };

export type AddPositionToMarginInstructionDataArgs = {};

export function getAddPositionToMarginInstructionDataEncoder(): FixedSizeEncoder<AddPositionToMarginInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({
      ...value,
      discriminator: ADD_POSITION_TO_MARGIN_DISCRIMINATOR,
    }),
  );
}

export function getAddPositionToMarginInstructionDataDecoder(): FixedSizeDecoder<AddPositionToMarginInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getAddPositionToMarginInstructionDataCodec(): FixedSizeCodec<
  AddPositionToMarginInstructionDataArgs,
  AddPositionToMarginInstructionData
> {
  return combineCodec(
    getAddPositionToMarginInstructionDataEncoder(),
    getAddPositionToMarginInstructionDataDecoder(),
  );
}

export type AddPositionToMarginAsyncInput<
  TAccountOwner extends string = string,
  TAccountMarginAccount extends string = string,
  TAccountPosition extends string = string,
  TAccountMarket extends string = string,
  TAccountOrderSlab extends string = string,
> = {
  /** Owner of both the group and position */
  owner: TransactionSigner<TAccountOwner>;
  /** Group to extend */
  marginAccount: Address<TAccountMarginAccount>;
  /** Flat position to bind (mode set to cross) */
  position: Address<TAccountPosition>;
  /** The position's market */
  market: Address<TAccountMarket>;
  /** OrderSlab scanned to reject an in-flight order */
  orderSlab?: Address<TAccountOrderSlab>;
};

export async function getAddPositionToMarginInstructionAsync<
  TAccountOwner extends string,
  TAccountMarginAccount extends string,
  TAccountPosition extends string,
  TAccountMarket extends string,
  TAccountOrderSlab extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: AddPositionToMarginAsyncInput<
    TAccountOwner,
    TAccountMarginAccount,
    TAccountPosition,
    TAccountMarket,
    TAccountOrderSlab
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AddPositionToMarginInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountMarginAccount,
    TAccountPosition,
    TAccountMarket,
    TAccountOrderSlab
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    marginAccount: { value: input.marginAccount ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    market: { value: input.market ?? null, isWritable: false },
    orderSlab: { value: input.orderSlab ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.orderSlab.value) {
    accounts.orderSlab.value = await findOrderSlabHeaderPda({
      market: getAddressFromResolvedInstructionAccount(
        "market",
        accounts.market.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("marginAccount", accounts.marginAccount),
      getAccountMeta("position", accounts.position),
      getAccountMeta("market", accounts.market),
      getAccountMeta("orderSlab", accounts.orderSlab),
    ],
    data: getAddPositionToMarginInstructionDataEncoder().encode({}),
    programAddress,
  } as AddPositionToMarginInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountMarginAccount,
    TAccountPosition,
    TAccountMarket,
    TAccountOrderSlab
  >);
}

export type AddPositionToMarginInput<
  TAccountOwner extends string = string,
  TAccountMarginAccount extends string = string,
  TAccountPosition extends string = string,
  TAccountMarket extends string = string,
  TAccountOrderSlab extends string = string,
> = {
  /** Owner of both the group and position */
  owner: TransactionSigner<TAccountOwner>;
  /** Group to extend */
  marginAccount: Address<TAccountMarginAccount>;
  /** Flat position to bind (mode set to cross) */
  position: Address<TAccountPosition>;
  /** The position's market */
  market: Address<TAccountMarket>;
  /** OrderSlab scanned to reject an in-flight order */
  orderSlab: Address<TAccountOrderSlab>;
};

export function getAddPositionToMarginInstruction<
  TAccountOwner extends string,
  TAccountMarginAccount extends string,
  TAccountPosition extends string,
  TAccountMarket extends string,
  TAccountOrderSlab extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: AddPositionToMarginInput<
    TAccountOwner,
    TAccountMarginAccount,
    TAccountPosition,
    TAccountMarket,
    TAccountOrderSlab
  >,
  config?: { programAddress?: TProgramAddress },
): AddPositionToMarginInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountMarginAccount,
  TAccountPosition,
  TAccountMarket,
  TAccountOrderSlab
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    marginAccount: { value: input.marginAccount ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    market: { value: input.market ?? null, isWritable: false },
    orderSlab: { value: input.orderSlab ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("marginAccount", accounts.marginAccount),
      getAccountMeta("position", accounts.position),
      getAccountMeta("market", accounts.market),
      getAccountMeta("orderSlab", accounts.orderSlab),
    ],
    data: getAddPositionToMarginInstructionDataEncoder().encode({}),
    programAddress,
  } as AddPositionToMarginInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountMarginAccount,
    TAccountPosition,
    TAccountMarket,
    TAccountOrderSlab
  >);
}

export type ParsedAddPositionToMarginInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Owner of both the group and position */
    owner: TAccountMetas[0];
    /** Group to extend */
    marginAccount: TAccountMetas[1];
    /** Flat position to bind (mode set to cross) */
    position: TAccountMetas[2];
    /** The position's market */
    market: TAccountMetas[3];
    /** OrderSlab scanned to reject an in-flight order */
    orderSlab: TAccountMetas[4];
  };
  data: AddPositionToMarginInstructionData;
};

export function parseAddPositionToMarginInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddPositionToMarginInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      marginAccount: getNextAccount(),
      position: getNextAccount(),
      market: getNextAccount(),
      orderSlab: getNextAccount(),
    },
    data: getAddPositionToMarginInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}

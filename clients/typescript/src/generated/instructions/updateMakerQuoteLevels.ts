/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_MAKER_QUOTE_LEVELS_DISCRIMINATOR = 18;

export function getUpdateMakerQuoteLevelsDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(UPDATE_MAKER_QUOTE_LEVELS_DISCRIMINATOR);
}

export type UpdateMakerQuoteLevelsInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountWriter extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountMakerQuote extends string | AccountMeta<string> = string,
  TAccountUserCollateral extends string | AccountMeta<string> | undefined =
    undefined,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountWriter extends string
        ? ReadonlySignerAccount<TAccountWriter> &
            AccountSignerMeta<TAccountWriter>
        : TAccountWriter,
      TAccountMarket extends string
        ? ReadonlyAccount<TAccountMarket>
        : TAccountMarket,
      TAccountMakerQuote extends string
        ? WritableAccount<TAccountMakerQuote>
        : TAccountMakerQuote,
      ...(TAccountUserCollateral extends undefined
        ? []
        : [
            TAccountUserCollateral extends string
              ? WritableAccount<TAccountUserCollateral>
              : TAccountUserCollateral,
          ]),
      ...TRemainingAccounts,
    ]
  >;

export type UpdateMakerQuoteLevelsInstructionData = {
  discriminator: number;
  sequence: bigint;
  midTick: number;
  numBids: number;
  numAsks: number;
  bidLevels: Array<number>;
  askLevels: Array<number>;
};

export type UpdateMakerQuoteLevelsInstructionDataArgs = {
  sequence: number | bigint;
  midTick: number;
  numBids: number;
  numAsks: number;
  bidLevels: Array<number>;
  askLevels: Array<number>;
};

export function getUpdateMakerQuoteLevelsInstructionDataEncoder(): FixedSizeEncoder<UpdateMakerQuoteLevelsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["sequence", getU64Encoder()],
      ["midTick", getU32Encoder()],
      ["numBids", getU8Encoder()],
      ["numAsks", getU8Encoder()],
      ["bidLevels", getArrayEncoder(getU8Encoder(), { size: 80 })],
      ["askLevels", getArrayEncoder(getU8Encoder(), { size: 80 })],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_MAKER_QUOTE_LEVELS_DISCRIMINATOR,
    }),
  );
}

export function getUpdateMakerQuoteLevelsInstructionDataDecoder(): FixedSizeDecoder<UpdateMakerQuoteLevelsInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["sequence", getU64Decoder()],
    ["midTick", getU32Decoder()],
    ["numBids", getU8Decoder()],
    ["numAsks", getU8Decoder()],
    ["bidLevels", getArrayDecoder(getU8Decoder(), { size: 80 })],
    ["askLevels", getArrayDecoder(getU8Decoder(), { size: 80 })],
  ]);
}

export function getUpdateMakerQuoteLevelsInstructionDataCodec(): FixedSizeCodec<
  UpdateMakerQuoteLevelsInstructionDataArgs,
  UpdateMakerQuoteLevelsInstructionData
> {
  return combineCodec(
    getUpdateMakerQuoteLevelsInstructionDataEncoder(),
    getUpdateMakerQuoteLevelsInstructionDataDecoder(),
  );
}

export type UpdateMakerQuoteLevelsInput<
  TAccountWriter extends string = string,
  TAccountMarket extends string = string,
  TAccountMakerQuote extends string = string,
  TAccountUserCollateral extends string = string,
> = {
  /** Maker or its delegate */
  writer: TransactionSigner<TAccountWriter>;
  /** Market (supplies num_ticks) */
  market: Address<TAccountMarket>;
  /** MakerQuote to update */
  makerQuote: Address<TAccountMakerQuote>;
  /** (Optional) the MAKER's mint-scoped collateral ledger (reservation delta-locked here); REQUIRED on a money-path market, omitted on clearing-only */
  userCollateral?: Address<TAccountUserCollateral>;
  sequence: UpdateMakerQuoteLevelsInstructionDataArgs["sequence"];
  midTick: UpdateMakerQuoteLevelsInstructionDataArgs["midTick"];
  numBids: UpdateMakerQuoteLevelsInstructionDataArgs["numBids"];
  numAsks: UpdateMakerQuoteLevelsInstructionDataArgs["numAsks"];
  bidLevels: UpdateMakerQuoteLevelsInstructionDataArgs["bidLevels"];
  askLevels: UpdateMakerQuoteLevelsInstructionDataArgs["askLevels"];
};

export function getUpdateMakerQuoteLevelsInstruction<
  TAccountWriter extends string,
  TAccountMarket extends string,
  TAccountMakerQuote extends string,
  TAccountUserCollateral extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: UpdateMakerQuoteLevelsInput<
    TAccountWriter,
    TAccountMarket,
    TAccountMakerQuote,
    TAccountUserCollateral
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateMakerQuoteLevelsInstruction<
  TProgramAddress,
  TAccountWriter,
  TAccountMarket,
  TAccountMakerQuote,
  TAccountUserCollateral
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    writer: { value: input.writer ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: false },
    makerQuote: { value: input.makerQuote ?? null, isWritable: true },
    userCollateral: { value: input.userCollateral ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("writer", accounts.writer),
      getAccountMeta("market", accounts.market),
      getAccountMeta("makerQuote", accounts.makerQuote),
      getAccountMeta("userCollateral", accounts.userCollateral),
    ].filter(<T>(x: T | undefined): x is T => x !== undefined),
    data: getUpdateMakerQuoteLevelsInstructionDataEncoder().encode(
      args as UpdateMakerQuoteLevelsInstructionDataArgs,
    ),
    programAddress,
  } as UpdateMakerQuoteLevelsInstruction<
    TProgramAddress,
    TAccountWriter,
    TAccountMarket,
    TAccountMakerQuote,
    TAccountUserCollateral
  >);
}

export type ParsedUpdateMakerQuoteLevelsInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Maker or its delegate */
    writer: TAccountMetas[0];
    /** Market (supplies num_ticks) */
    market: TAccountMetas[1];
    /** MakerQuote to update */
    makerQuote: TAccountMetas[2];
    /** (Optional) the MAKER's mint-scoped collateral ledger (reservation delta-locked here); REQUIRED on a money-path market, omitted on clearing-only */
    userCollateral?: TAccountMetas[3] | undefined;
  };
  data: UpdateMakerQuoteLevelsInstructionData;
};

export function parseUpdateMakerQuoteLevelsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateMakerQuoteLevelsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  let optionalAccountsRemaining = instruction.accounts.length - 3;
  const getNextOptionalAccount = () => {
    if (optionalAccountsRemaining === 0) return undefined;
    optionalAccountsRemaining -= 1;
    return getNextAccount();
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      writer: getNextAccount(),
      market: getNextAccount(),
      makerQuote: getNextAccount(),
      userCollateral: getNextOptionalAccount(),
    },
    data: getUpdateMakerQuoteLevelsInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}

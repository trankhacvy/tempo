/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findClearingResultPda, findOrderSlabHeaderPda } from "../pdas";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const SETTLE_MAKER_QUOTE_DISCRIMINATOR = 21;

export function getSettleMakerQuoteDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(SETTLE_MAKER_QUOTE_DISCRIMINATOR);
}

export type SettleMakerQuoteInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountCranker extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountClearingResult extends string | AccountMeta<string> = string,
  TAccountOrderSlab extends string | AccountMeta<string> = string,
  TAccountMakerQuote extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TAccountUserCollateral extends string | AccountMeta<string> | undefined =
    undefined,
  TAccountVault extends string | AccountMeta<string> | undefined = undefined,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCranker extends string
        ? ReadonlySignerAccount<TAccountCranker> &
            AccountSignerMeta<TAccountCranker>
        : TAccountCranker,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountClearingResult extends string
        ? ReadonlyAccount<TAccountClearingResult>
        : TAccountClearingResult,
      TAccountOrderSlab extends string
        ? ReadonlyAccount<TAccountOrderSlab>
        : TAccountOrderSlab,
      TAccountMakerQuote extends string
        ? WritableAccount<TAccountMakerQuote>
        : TAccountMakerQuote,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      ...(TAccountUserCollateral extends undefined
        ? []
        : [
            TAccountUserCollateral extends string
              ? WritableAccount<TAccountUserCollateral>
              : TAccountUserCollateral,
          ]),
      ...(TAccountVault extends undefined
        ? []
        : [
            TAccountVault extends string
              ? WritableAccount<TAccountVault>
              : TAccountVault,
          ]),
      ...TRemainingAccounts,
    ]
  >;

export type SettleMakerQuoteInstructionData = { discriminator: number };

export type SettleMakerQuoteInstructionDataArgs = {};

export function getSettleMakerQuoteInstructionDataEncoder(): FixedSizeEncoder<SettleMakerQuoteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: SETTLE_MAKER_QUOTE_DISCRIMINATOR }),
  );
}

export function getSettleMakerQuoteInstructionDataDecoder(): FixedSizeDecoder<SettleMakerQuoteInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getSettleMakerQuoteInstructionDataCodec(): FixedSizeCodec<
  SettleMakerQuoteInstructionDataArgs,
  SettleMakerQuoteInstructionData
> {
  return combineCodec(
    getSettleMakerQuoteInstructionDataEncoder(),
    getSettleMakerQuoteInstructionDataDecoder(),
  );
}

export type SettleMakerQuoteAsyncInput<
  TAccountCranker extends string = string,
  TAccountMarket extends string = string,
  TAccountClearingResult extends string = string,
  TAccountOrderSlab extends string = string,
  TAccountMakerQuote extends string = string,
  TAccountPosition extends string = string,
  TAccountUserCollateral extends string = string,
  TAccountVault extends string = string,
> = {
  /** Permissionless caller */
  cranker: TransactionSigner<TAccountCranker>;
  /** Market being settled */
  market: Address<TAccountMarket>;
  /** Published clearing result */
  clearingResult?: Address<TAccountClearingResult>;
  /** OrderSlab (scanned for marginal-tick orders) */
  orderSlab?: Address<TAccountOrderSlab>;
  /** MakerQuote to settle */
  makerQuote: Address<TAccountMakerQuote>;
  /** Maker's Position */
  position: Address<TAccountPosition>;
  /** (Optional) maker's collateral ledger */
  userCollateral?: Address<TAccountUserCollateral>;
  /** (Optional) fee/insurance pool */
  vault?: Address<TAccountVault>;
};

export async function getSettleMakerQuoteInstructionAsync<
  TAccountCranker extends string,
  TAccountMarket extends string,
  TAccountClearingResult extends string,
  TAccountOrderSlab extends string,
  TAccountMakerQuote extends string,
  TAccountPosition extends string,
  TAccountUserCollateral extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: SettleMakerQuoteAsyncInput<
    TAccountCranker,
    TAccountMarket,
    TAccountClearingResult,
    TAccountOrderSlab,
    TAccountMakerQuote,
    TAccountPosition,
    TAccountUserCollateral,
    TAccountVault
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SettleMakerQuoteInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountMarket,
    TAccountClearingResult,
    TAccountOrderSlab,
    TAccountMakerQuote,
    TAccountPosition,
    TAccountUserCollateral,
    TAccountVault
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    clearingResult: { value: input.clearingResult ?? null, isWritable: false },
    orderSlab: { value: input.orderSlab ?? null, isWritable: false },
    makerQuote: { value: input.makerQuote ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    userCollateral: { value: input.userCollateral ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.clearingResult.value) {
    accounts.clearingResult.value = await findClearingResultPda({
      market: getAddressFromResolvedInstructionAccount(
        "market",
        accounts.market.value,
      ),
    });
  }
  if (!accounts.orderSlab.value) {
    accounts.orderSlab.value = await findOrderSlabHeaderPda({
      market: getAddressFromResolvedInstructionAccount(
        "market",
        accounts.market.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("market", accounts.market),
      getAccountMeta("clearingResult", accounts.clearingResult),
      getAccountMeta("orderSlab", accounts.orderSlab),
      getAccountMeta("makerQuote", accounts.makerQuote),
      getAccountMeta("position", accounts.position),
      getAccountMeta("userCollateral", accounts.userCollateral),
      getAccountMeta("vault", accounts.vault),
    ].filter(<T>(x: T | undefined): x is T => x !== undefined),
    data: getSettleMakerQuoteInstructionDataEncoder().encode({}),
    programAddress,
  } as SettleMakerQuoteInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountMarket,
    TAccountClearingResult,
    TAccountOrderSlab,
    TAccountMakerQuote,
    TAccountPosition,
    TAccountUserCollateral,
    TAccountVault
  >);
}

export type SettleMakerQuoteInput<
  TAccountCranker extends string = string,
  TAccountMarket extends string = string,
  TAccountClearingResult extends string = string,
  TAccountOrderSlab extends string = string,
  TAccountMakerQuote extends string = string,
  TAccountPosition extends string = string,
  TAccountUserCollateral extends string = string,
  TAccountVault extends string = string,
> = {
  /** Permissionless caller */
  cranker: TransactionSigner<TAccountCranker>;
  /** Market being settled */
  market: Address<TAccountMarket>;
  /** Published clearing result */
  clearingResult: Address<TAccountClearingResult>;
  /** OrderSlab (scanned for marginal-tick orders) */
  orderSlab: Address<TAccountOrderSlab>;
  /** MakerQuote to settle */
  makerQuote: Address<TAccountMakerQuote>;
  /** Maker's Position */
  position: Address<TAccountPosition>;
  /** (Optional) maker's collateral ledger */
  userCollateral?: Address<TAccountUserCollateral>;
  /** (Optional) fee/insurance pool */
  vault?: Address<TAccountVault>;
};

export function getSettleMakerQuoteInstruction<
  TAccountCranker extends string,
  TAccountMarket extends string,
  TAccountClearingResult extends string,
  TAccountOrderSlab extends string,
  TAccountMakerQuote extends string,
  TAccountPosition extends string,
  TAccountUserCollateral extends string,
  TAccountVault extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: SettleMakerQuoteInput<
    TAccountCranker,
    TAccountMarket,
    TAccountClearingResult,
    TAccountOrderSlab,
    TAccountMakerQuote,
    TAccountPosition,
    TAccountUserCollateral,
    TAccountVault
  >,
  config?: { programAddress?: TProgramAddress },
): SettleMakerQuoteInstruction<
  TProgramAddress,
  TAccountCranker,
  TAccountMarket,
  TAccountClearingResult,
  TAccountOrderSlab,
  TAccountMakerQuote,
  TAccountPosition,
  TAccountUserCollateral,
  TAccountVault
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    clearingResult: { value: input.clearingResult ?? null, isWritable: false },
    orderSlab: { value: input.orderSlab ?? null, isWritable: false },
    makerQuote: { value: input.makerQuote ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    userCollateral: { value: input.userCollateral ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("market", accounts.market),
      getAccountMeta("clearingResult", accounts.clearingResult),
      getAccountMeta("orderSlab", accounts.orderSlab),
      getAccountMeta("makerQuote", accounts.makerQuote),
      getAccountMeta("position", accounts.position),
      getAccountMeta("userCollateral", accounts.userCollateral),
      getAccountMeta("vault", accounts.vault),
    ].filter(<T>(x: T | undefined): x is T => x !== undefined),
    data: getSettleMakerQuoteInstructionDataEncoder().encode({}),
    programAddress,
  } as SettleMakerQuoteInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountMarket,
    TAccountClearingResult,
    TAccountOrderSlab,
    TAccountMakerQuote,
    TAccountPosition,
    TAccountUserCollateral,
    TAccountVault
  >);
}

export type ParsedSettleMakerQuoteInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Permissionless caller */
    cranker: TAccountMetas[0];
    /** Market being settled */
    market: TAccountMetas[1];
    /** Published clearing result */
    clearingResult: TAccountMetas[2];
    /** OrderSlab (scanned for marginal-tick orders) */
    orderSlab: TAccountMetas[3];
    /** MakerQuote to settle */
    makerQuote: TAccountMetas[4];
    /** Maker's Position */
    position: TAccountMetas[5];
    /** (Optional) maker's collateral ledger */
    userCollateral?: TAccountMetas[6] | undefined;
    /** (Optional) fee/insurance pool */
    vault?: TAccountMetas[7] | undefined;
  };
  data: SettleMakerQuoteInstructionData;
};

export function parseSettleMakerQuoteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSettleMakerQuoteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  let optionalAccountsRemaining = instruction.accounts.length - 6;
  const getNextOptionalAccount = () => {
    if (optionalAccountsRemaining === 0) return undefined;
    optionalAccountsRemaining -= 1;
    return getNextAccount();
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      cranker: getNextAccount(),
      market: getNextAccount(),
      clearingResult: getNextAccount(),
      orderSlab: getNextAccount(),
      makerQuote: getNextAccount(),
      position: getNextAccount(),
      userCollateral: getNextOptionalAccount(),
      vault: getNextOptionalAccount(),
    },
    data: getSettleMakerQuoteInstructionDataDecoder().decode(instruction.data),
  };
}

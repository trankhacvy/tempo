/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const MIGRATE_MARKET_DISCRIMINATOR = 26;

export function getMigrateMarketDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(MIGRATE_MARKET_DISCRIMINATOR);
}

export type MigrateMarketInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MigrateMarketInstructionData = {
  discriminator: number;
  maxPriceMoveBpsPerSlot: number;
  softStaleSlots: bigint;
};

export type MigrateMarketInstructionDataArgs = {
  maxPriceMoveBpsPerSlot: number;
  softStaleSlots: number | bigint;
};

export function getMigrateMarketInstructionDataEncoder(): FixedSizeEncoder<MigrateMarketInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["maxPriceMoveBpsPerSlot", getU16Encoder()],
      ["softStaleSlots", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: MIGRATE_MARKET_DISCRIMINATOR }),
  );
}

export function getMigrateMarketInstructionDataDecoder(): FixedSizeDecoder<MigrateMarketInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["maxPriceMoveBpsPerSlot", getU16Decoder()],
    ["softStaleSlots", getU64Decoder()],
  ]);
}

export function getMigrateMarketInstructionDataCodec(): FixedSizeCodec<
  MigrateMarketInstructionDataArgs,
  MigrateMarketInstructionData
> {
  return combineCodec(
    getMigrateMarketInstructionDataEncoder(),
    getMigrateMarketInstructionDataDecoder(),
  );
}

export type MigrateMarketInput<
  TAccountAuthority extends string = string,
  TAccountMarket extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Market authority (admin) */
  authority: TransactionSigner<TAccountAuthority>;
  /** Market account to upgrade */
  market: Address<TAccountMarket>;
  /** Funds the grown-account rent */
  payer: TransactionSigner<TAccountPayer>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
  maxPriceMoveBpsPerSlot: MigrateMarketInstructionDataArgs["maxPriceMoveBpsPerSlot"];
  softStaleSlots: MigrateMarketInstructionDataArgs["softStaleSlots"];
};

export function getMigrateMarketInstruction<
  TAccountAuthority extends string,
  TAccountMarket extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: MigrateMarketInput<
    TAccountAuthority,
    TAccountMarket,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): MigrateMarketInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountMarket,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("market", accounts.market),
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getMigrateMarketInstructionDataEncoder().encode(
      args as MigrateMarketInstructionDataArgs,
    ),
    programAddress,
  } as MigrateMarketInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountMarket,
    TAccountPayer,
    TAccountSystemProgram
  >);
}

export type ParsedMigrateMarketInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Market authority (admin) */
    authority: TAccountMetas[0];
    /** Market account to upgrade */
    market: TAccountMetas[1];
    /** Funds the grown-account rent */
    payer: TAccountMetas[2];
    /** System program */
    systemProgram: TAccountMetas[3];
  };
  data: MigrateMarketInstructionData;
};

export function parseMigrateMarketInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMigrateMarketInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      market: getNextAccount(),
      payer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getMigrateMarketInstructionDataDecoder().decode(instruction.data),
  };
}

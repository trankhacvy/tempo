/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ProgramDerivedAddress,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getResolvedInstructionAccountAsProgramDerivedAddress,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const INIT_COLLATERAL_DISCRIMINATOR = 10;

export function getInitCollateralDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(INIT_COLLATERAL_DISCRIMINATOR);
}

export type InitCollateralInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountUserCollateral extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountUserCollateral extends string
        ? WritableAccount<TAccountUserCollateral>
        : TAccountUserCollateral,
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitCollateralInstructionData = {
  discriminator: number;
  bump: number;
};

export type InitCollateralInstructionDataArgs = { bump: number };

export function getInitCollateralInstructionDataEncoder(): FixedSizeEncoder<InitCollateralInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_COLLATERAL_DISCRIMINATOR }),
  );
}

export function getInitCollateralInstructionDataDecoder(): FixedSizeDecoder<InitCollateralInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

export function getInitCollateralInstructionDataCodec(): FixedSizeCodec<
  InitCollateralInstructionDataArgs,
  InitCollateralInstructionData
> {
  return combineCodec(
    getInitCollateralInstructionDataEncoder(),
    getInitCollateralInstructionDataDecoder(),
  );
}

export type InitCollateralInput<
  TAccountPayer extends string = string,
  TAccountOwner extends string = string,
  TAccountUserCollateral extends string = string,
  TAccountVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pays for the ledger account */
  payer: TransactionSigner<TAccountPayer>;
  /** Trader the ledger belongs to */
  owner: TransactionSigner<TAccountOwner>;
  /** UserCollateral PDA to create (seeds [collateral, owner, vault.collateral_mint]) */
  userCollateral: ProgramDerivedAddress<TAccountUserCollateral>;
  /** Per-collateral vault whose collateral_mint scopes the ledger (CR-3) */
  vault: Address<TAccountVault>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
  bump?: InitCollateralInstructionDataArgs["bump"];
};

export function getInitCollateralInstruction<
  TAccountPayer extends string,
  TAccountOwner extends string,
  TAccountUserCollateral extends string,
  TAccountVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: InitCollateralInput<
    TAccountPayer,
    TAccountOwner,
    TAccountUserCollateral,
    TAccountVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitCollateralInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountOwner,
  TAccountUserCollateral,
  TAccountVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    userCollateral: { value: input.userCollateral ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!args.bump) {
    args.bump = getResolvedInstructionAccountAsProgramDerivedAddress(
      "userCollateral",
      accounts.userCollateral.value,
    )[1];
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("userCollateral", accounts.userCollateral),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitCollateralInstructionDataEncoder().encode(
      args as InitCollateralInstructionDataArgs,
    ),
    programAddress,
  } as InitCollateralInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountUserCollateral,
    TAccountVault,
    TAccountSystemProgram
  >);
}

export type ParsedInitCollateralInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pays for the ledger account */
    payer: TAccountMetas[0];
    /** Trader the ledger belongs to */
    owner: TAccountMetas[1];
    /** UserCollateral PDA to create (seeds [collateral, owner, vault.collateral_mint]) */
    userCollateral: TAccountMetas[2];
    /** Per-collateral vault whose collateral_mint scopes the ledger (CR-3) */
    vault: TAccountMetas[3];
    /** System program */
    systemProgram: TAccountMetas[4];
  };
  data: InitCollateralInstructionData;
};

export function parseInitCollateralInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitCollateralInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      owner: getNextAccount(),
      userCollateral: getNextAccount(),
      vault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitCollateralInstructionDataDecoder().decode(instruction.data),
  };
}

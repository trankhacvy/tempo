/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getArrayDecoder,
  getArrayEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const PROPOSE_SET_ORACLE_DISCRIMINATOR = 38;

export function getProposeSetOracleDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(PROPOSE_SET_ORACLE_DISCRIMINATOR);
}

export type ProposeSetOracleInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeSetOracleInstructionData = {
  discriminator: number;
  newOracle: Array<number>;
  newFeedId: Array<number>;
};

export type ProposeSetOracleInstructionDataArgs = {
  newOracle: Array<number>;
  newFeedId: Array<number>;
};

export function getProposeSetOracleInstructionDataEncoder(): FixedSizeEncoder<ProposeSetOracleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["newOracle", getArrayEncoder(getU8Encoder(), { size: 32 })],
      ["newFeedId", getArrayEncoder(getU8Encoder(), { size: 32 })],
    ]),
    (value) => ({ ...value, discriminator: PROPOSE_SET_ORACLE_DISCRIMINATOR }),
  );
}

export function getProposeSetOracleInstructionDataDecoder(): FixedSizeDecoder<ProposeSetOracleInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["newOracle", getArrayDecoder(getU8Decoder(), { size: 32 })],
    ["newFeedId", getArrayDecoder(getU8Decoder(), { size: 32 })],
  ]);
}

export function getProposeSetOracleInstructionDataCodec(): FixedSizeCodec<
  ProposeSetOracleInstructionDataArgs,
  ProposeSetOracleInstructionData
> {
  return combineCodec(
    getProposeSetOracleInstructionDataEncoder(),
    getProposeSetOracleInstructionDataDecoder(),
  );
}

export type ProposeSetOracleInput<
  TAccountAuthority extends string = string,
  TAccountMarket extends string = string,
> = {
  /** Market authority */
  authority: TransactionSigner<TAccountAuthority>;
  /** Market (staging slot written) */
  market: Address<TAccountMarket>;
  newOracle: ProposeSetOracleInstructionDataArgs["newOracle"];
  newFeedId: ProposeSetOracleInstructionDataArgs["newFeedId"];
};

export function getProposeSetOracleInstruction<
  TAccountAuthority extends string,
  TAccountMarket extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ProposeSetOracleInput<TAccountAuthority, TAccountMarket>,
  config?: { programAddress?: TProgramAddress },
): ProposeSetOracleInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountMarket
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("market", accounts.market),
    ],
    data: getProposeSetOracleInstructionDataEncoder().encode(
      args as ProposeSetOracleInstructionDataArgs,
    ),
    programAddress,
  } as ProposeSetOracleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountMarket
  >);
}

export type ParsedProposeSetOracleInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Market authority */
    authority: TAccountMetas[0];
    /** Market (staging slot written) */
    market: TAccountMetas[1];
  };
  data: ProposeSetOracleInstructionData;
};

export function parseProposeSetOracleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposeSetOracleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), market: getNextAccount() },
    data: getProposeSetOracleInstructionDataDecoder().decode(instruction.data),
  };
}

/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_MAKER_QUOTE_MID_DISCRIMINATOR = 17;

export function getUpdateMakerQuoteMidDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(UPDATE_MAKER_QUOTE_MID_DISCRIMINATOR);
}

export type UpdateMakerQuoteMidInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountWriter extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountMakerQuote extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountWriter extends string
        ? ReadonlySignerAccount<TAccountWriter> &
            AccountSignerMeta<TAccountWriter>
        : TAccountWriter,
      TAccountMarket extends string
        ? ReadonlyAccount<TAccountMarket>
        : TAccountMarket,
      TAccountMakerQuote extends string
        ? WritableAccount<TAccountMakerQuote>
        : TAccountMakerQuote,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateMakerQuoteMidInstructionData = {
  discriminator: number;
  sequence: bigint;
  midTick: number;
};

export type UpdateMakerQuoteMidInstructionDataArgs = {
  sequence: number | bigint;
  midTick: number;
};

export function getUpdateMakerQuoteMidInstructionDataEncoder(): FixedSizeEncoder<UpdateMakerQuoteMidInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["sequence", getU64Encoder()],
      ["midTick", getU32Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_MAKER_QUOTE_MID_DISCRIMINATOR,
    }),
  );
}

export function getUpdateMakerQuoteMidInstructionDataDecoder(): FixedSizeDecoder<UpdateMakerQuoteMidInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["sequence", getU64Decoder()],
    ["midTick", getU32Decoder()],
  ]);
}

export function getUpdateMakerQuoteMidInstructionDataCodec(): FixedSizeCodec<
  UpdateMakerQuoteMidInstructionDataArgs,
  UpdateMakerQuoteMidInstructionData
> {
  return combineCodec(
    getUpdateMakerQuoteMidInstructionDataEncoder(),
    getUpdateMakerQuoteMidInstructionDataDecoder(),
  );
}

export type UpdateMakerQuoteMidInput<
  TAccountWriter extends string = string,
  TAccountMarket extends string = string,
  TAccountMakerQuote extends string = string,
> = {
  /** Maker or its delegate */
  writer: TransactionSigner<TAccountWriter>;
  /** Market (supplies num_ticks) */
  market: Address<TAccountMarket>;
  /** MakerQuote to update */
  makerQuote: Address<TAccountMakerQuote>;
  sequence: UpdateMakerQuoteMidInstructionDataArgs["sequence"];
  midTick: UpdateMakerQuoteMidInstructionDataArgs["midTick"];
};

export function getUpdateMakerQuoteMidInstruction<
  TAccountWriter extends string,
  TAccountMarket extends string,
  TAccountMakerQuote extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: UpdateMakerQuoteMidInput<
    TAccountWriter,
    TAccountMarket,
    TAccountMakerQuote
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateMakerQuoteMidInstruction<
  TProgramAddress,
  TAccountWriter,
  TAccountMarket,
  TAccountMakerQuote
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    writer: { value: input.writer ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: false },
    makerQuote: { value: input.makerQuote ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("writer", accounts.writer),
      getAccountMeta("market", accounts.market),
      getAccountMeta("makerQuote", accounts.makerQuote),
    ],
    data: getUpdateMakerQuoteMidInstructionDataEncoder().encode(
      args as UpdateMakerQuoteMidInstructionDataArgs,
    ),
    programAddress,
  } as UpdateMakerQuoteMidInstruction<
    TProgramAddress,
    TAccountWriter,
    TAccountMarket,
    TAccountMakerQuote
  >);
}

export type ParsedUpdateMakerQuoteMidInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Maker or its delegate */
    writer: TAccountMetas[0];
    /** Market (supplies num_ticks) */
    market: TAccountMetas[1];
    /** MakerQuote to update */
    makerQuote: TAccountMetas[2];
  };
  data: UpdateMakerQuoteMidInstructionData;
};

export function parseUpdateMakerQuoteMidInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateMakerQuoteMidInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      writer: getNextAccount(),
      market: getNextAccount(),
      makerQuote: getNextAccount(),
    },
    data: getUpdateMakerQuoteMidInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}

/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const INIT_MARGIN_ACCOUNT_DISCRIMINATOR = 22;

export function getInitMarginAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(INIT_MARGIN_ACCOUNT_DISCRIMINATOR);
}

export type InitMarginAccountInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountMarginAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountMarginAccount extends string
        ? WritableAccount<TAccountMarginAccount>
        : TAccountMarginAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitMarginAccountInstructionData = {
  discriminator: number;
  marginBump: number;
};

export type InitMarginAccountInstructionDataArgs = { marginBump: number };

export function getInitMarginAccountInstructionDataEncoder(): FixedSizeEncoder<InitMarginAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["marginBump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_MARGIN_ACCOUNT_DISCRIMINATOR }),
  );
}

export function getInitMarginAccountInstructionDataDecoder(): FixedSizeDecoder<InitMarginAccountInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["marginBump", getU8Decoder()],
  ]);
}

export function getInitMarginAccountInstructionDataCodec(): FixedSizeCodec<
  InitMarginAccountInstructionDataArgs,
  InitMarginAccountInstructionData
> {
  return combineCodec(
    getInitMarginAccountInstructionDataEncoder(),
    getInitMarginAccountInstructionDataDecoder(),
  );
}

export type InitMarginAccountInput<
  TAccountPayer extends string = string,
  TAccountOwner extends string = string,
  TAccountMarginAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pays for the group account */
  payer: TransactionSigner<TAccountPayer>;
  /** Owner the group belongs to */
  owner: TransactionSigner<TAccountOwner>;
  /** MarginAccount PDA to create */
  marginAccount: Address<TAccountMarginAccount>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
  marginBump: InitMarginAccountInstructionDataArgs["marginBump"];
};

export function getInitMarginAccountInstruction<
  TAccountPayer extends string,
  TAccountOwner extends string,
  TAccountMarginAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: InitMarginAccountInput<
    TAccountPayer,
    TAccountOwner,
    TAccountMarginAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitMarginAccountInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountOwner,
  TAccountMarginAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    marginAccount: { value: input.marginAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("marginAccount", accounts.marginAccount),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitMarginAccountInstructionDataEncoder().encode(
      args as InitMarginAccountInstructionDataArgs,
    ),
    programAddress,
  } as InitMarginAccountInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountMarginAccount,
    TAccountSystemProgram
  >);
}

export type ParsedInitMarginAccountInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pays for the group account */
    payer: TAccountMetas[0];
    /** Owner the group belongs to */
    owner: TAccountMetas[1];
    /** MarginAccount PDA to create */
    marginAccount: TAccountMetas[2];
    /** System program */
    systemProgram: TAccountMetas[3];
  };
  data: InitMarginAccountInstructionData;
};

export function parseInitMarginAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitMarginAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      owner: getNextAccount(),
      marginAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitMarginAccountInstructionDataDecoder().decode(instruction.data),
  };
}

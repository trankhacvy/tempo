/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAuctionHistogramHeaderPda } from "../pdas";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const PROCESS_MAKER_QUOTE_DISCRIMINATOR = 20;

export function getProcessMakerQuoteDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(PROCESS_MAKER_QUOTE_DISCRIMINATOR);
}

export type ProcessMakerQuoteInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountCranker extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountHistogram extends string | AccountMeta<string> = string,
  TAccountMakerQuote extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCranker extends string
        ? ReadonlySignerAccount<TAccountCranker> &
            AccountSignerMeta<TAccountCranker>
        : TAccountCranker,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountHistogram extends string
        ? WritableAccount<TAccountHistogram>
        : TAccountHistogram,
      TAccountMakerQuote extends string
        ? WritableAccount<TAccountMakerQuote>
        : TAccountMakerQuote,
      ...TRemainingAccounts,
    ]
  >;

export type ProcessMakerQuoteInstructionData = { discriminator: number };

export type ProcessMakerQuoteInstructionDataArgs = {};

export function getProcessMakerQuoteInstructionDataEncoder(): FixedSizeEncoder<ProcessMakerQuoteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: PROCESS_MAKER_QUOTE_DISCRIMINATOR }),
  );
}

export function getProcessMakerQuoteInstructionDataDecoder(): FixedSizeDecoder<ProcessMakerQuoteInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getProcessMakerQuoteInstructionDataCodec(): FixedSizeCodec<
  ProcessMakerQuoteInstructionDataArgs,
  ProcessMakerQuoteInstructionData
> {
  return combineCodec(
    getProcessMakerQuoteInstructionDataEncoder(),
    getProcessMakerQuoteInstructionDataDecoder(),
  );
}

export type ProcessMakerQuoteAsyncInput<
  TAccountCranker extends string = string,
  TAccountMarket extends string = string,
  TAccountHistogram extends string = string,
  TAccountMakerQuote extends string = string,
> = {
  /** Permissionless caller */
  cranker: TransactionSigner<TAccountCranker>;
  /** Market being accumulated */
  market: Address<TAccountMarket>;
  /** AuctionHistogram fold target */
  histogram?: Address<TAccountHistogram>;
  /** MakerQuote to fold */
  makerQuote: Address<TAccountMakerQuote>;
};

export async function getProcessMakerQuoteInstructionAsync<
  TAccountCranker extends string,
  TAccountMarket extends string,
  TAccountHistogram extends string,
  TAccountMakerQuote extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ProcessMakerQuoteAsyncInput<
    TAccountCranker,
    TAccountMarket,
    TAccountHistogram,
    TAccountMakerQuote
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ProcessMakerQuoteInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountMarket,
    TAccountHistogram,
    TAccountMakerQuote
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    histogram: { value: input.histogram ?? null, isWritable: true },
    makerQuote: { value: input.makerQuote ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.histogram.value) {
    accounts.histogram.value = await findAuctionHistogramHeaderPda({
      market: getAddressFromResolvedInstructionAccount(
        "market",
        accounts.market.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("market", accounts.market),
      getAccountMeta("histogram", accounts.histogram),
      getAccountMeta("makerQuote", accounts.makerQuote),
    ],
    data: getProcessMakerQuoteInstructionDataEncoder().encode({}),
    programAddress,
  } as ProcessMakerQuoteInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountMarket,
    TAccountHistogram,
    TAccountMakerQuote
  >);
}

export type ProcessMakerQuoteInput<
  TAccountCranker extends string = string,
  TAccountMarket extends string = string,
  TAccountHistogram extends string = string,
  TAccountMakerQuote extends string = string,
> = {
  /** Permissionless caller */
  cranker: TransactionSigner<TAccountCranker>;
  /** Market being accumulated */
  market: Address<TAccountMarket>;
  /** AuctionHistogram fold target */
  histogram: Address<TAccountHistogram>;
  /** MakerQuote to fold */
  makerQuote: Address<TAccountMakerQuote>;
};

export function getProcessMakerQuoteInstruction<
  TAccountCranker extends string,
  TAccountMarket extends string,
  TAccountHistogram extends string,
  TAccountMakerQuote extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ProcessMakerQuoteInput<
    TAccountCranker,
    TAccountMarket,
    TAccountHistogram,
    TAccountMakerQuote
  >,
  config?: { programAddress?: TProgramAddress },
): ProcessMakerQuoteInstruction<
  TProgramAddress,
  TAccountCranker,
  TAccountMarket,
  TAccountHistogram,
  TAccountMakerQuote
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    histogram: { value: input.histogram ?? null, isWritable: true },
    makerQuote: { value: input.makerQuote ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("market", accounts.market),
      getAccountMeta("histogram", accounts.histogram),
      getAccountMeta("makerQuote", accounts.makerQuote),
    ],
    data: getProcessMakerQuoteInstructionDataEncoder().encode({}),
    programAddress,
  } as ProcessMakerQuoteInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountMarket,
    TAccountHistogram,
    TAccountMakerQuote
  >);
}

export type ParsedProcessMakerQuoteInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Permissionless caller */
    cranker: TAccountMetas[0];
    /** Market being accumulated */
    market: TAccountMetas[1];
    /** AuctionHistogram fold target */
    histogram: TAccountMetas[2];
    /** MakerQuote to fold */
    makerQuote: TAccountMetas[3];
  };
  data: ProcessMakerQuoteInstructionData;
};

export function parseProcessMakerQuoteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProcessMakerQuoteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      cranker: getNextAccount(),
      market: getNextAccount(),
      histogram: getNextAccount(),
      makerQuote: getNextAccount(),
    },
    data: getProcessMakerQuoteInstructionDataDecoder().decode(instruction.data),
  };
}

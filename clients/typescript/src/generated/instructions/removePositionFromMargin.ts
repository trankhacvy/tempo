/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const REMOVE_POSITION_FROM_MARGIN_DISCRIMINATOR = 28;

export function getRemovePositionFromMarginDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(REMOVE_POSITION_FROM_MARGIN_DISCRIMINATOR);
}

export type RemovePositionFromMarginInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountMarginAccount extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountMarginAccount extends string
        ? WritableAccount<TAccountMarginAccount>
        : TAccountMarginAccount,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      ...TRemainingAccounts,
    ]
  >;

export type RemovePositionFromMarginInstructionData = { discriminator: number };

export type RemovePositionFromMarginInstructionDataArgs = {};

export function getRemovePositionFromMarginInstructionDataEncoder(): FixedSizeEncoder<RemovePositionFromMarginInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({
      ...value,
      discriminator: REMOVE_POSITION_FROM_MARGIN_DISCRIMINATOR,
    }),
  );
}

export function getRemovePositionFromMarginInstructionDataDecoder(): FixedSizeDecoder<RemovePositionFromMarginInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getRemovePositionFromMarginInstructionDataCodec(): FixedSizeCodec<
  RemovePositionFromMarginInstructionDataArgs,
  RemovePositionFromMarginInstructionData
> {
  return combineCodec(
    getRemovePositionFromMarginInstructionDataEncoder(),
    getRemovePositionFromMarginInstructionDataDecoder(),
  );
}

export type RemovePositionFromMarginInput<
  TAccountOwner extends string = string,
  TAccountMarginAccount extends string = string,
  TAccountPosition extends string = string,
> = {
  /** Owner of both the group and position */
  owner: TransactionSigner<TAccountOwner>;
  /** Group to shrink */
  marginAccount: Address<TAccountMarginAccount>;
  /** Flat member position to unbind (mode set to isolated) */
  position: Address<TAccountPosition>;
};

export function getRemovePositionFromMarginInstruction<
  TAccountOwner extends string,
  TAccountMarginAccount extends string,
  TAccountPosition extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: RemovePositionFromMarginInput<
    TAccountOwner,
    TAccountMarginAccount,
    TAccountPosition
  >,
  config?: { programAddress?: TProgramAddress },
): RemovePositionFromMarginInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountMarginAccount,
  TAccountPosition
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    marginAccount: { value: input.marginAccount ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("marginAccount", accounts.marginAccount),
      getAccountMeta("position", accounts.position),
    ],
    data: getRemovePositionFromMarginInstructionDataEncoder().encode({}),
    programAddress,
  } as RemovePositionFromMarginInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountMarginAccount,
    TAccountPosition
  >);
}

export type ParsedRemovePositionFromMarginInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Owner of both the group and position */
    owner: TAccountMetas[0];
    /** Group to shrink */
    marginAccount: TAccountMetas[1];
    /** Flat member position to unbind (mode set to isolated) */
    position: TAccountMetas[2];
  };
  data: RemovePositionFromMarginInstructionData;
};

export function parseRemovePositionFromMarginInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemovePositionFromMarginInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      marginAccount: getNextAccount(),
      position: getNextAccount(),
    },
    data: getRemovePositionFromMarginInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}

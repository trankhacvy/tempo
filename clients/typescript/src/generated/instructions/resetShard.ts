/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const RESET_SHARD_DISCRIMINATOR = 31;

export function getResetShardDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(RESET_SHARD_DISCRIMINATOR);
}

export type ResetShardInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountCranker extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountOrderSlab extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCranker extends string
        ? ReadonlySignerAccount<TAccountCranker> &
            AccountSignerMeta<TAccountCranker>
        : TAccountCranker,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountOrderSlab extends string
        ? WritableAccount<TAccountOrderSlab>
        : TAccountOrderSlab,
      ...TRemainingAccounts,
    ]
  >;

export type ResetShardInstructionData = { discriminator: number };

export type ResetShardInstructionDataArgs = {};

export function getResetShardInstructionDataEncoder(): FixedSizeEncoder<ResetShardInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: RESET_SHARD_DISCRIMINATOR }),
  );
}

export function getResetShardInstructionDataDecoder(): FixedSizeDecoder<ResetShardInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getResetShardInstructionDataCodec(): FixedSizeCodec<
  ResetShardInstructionDataArgs,
  ResetShardInstructionData
> {
  return combineCodec(
    getResetShardInstructionDataEncoder(),
    getResetShardInstructionDataDecoder(),
  );
}

export type ResetShardInput<
  TAccountCranker extends string = string,
  TAccountMarket extends string = string,
  TAccountOrderSlab extends string = string,
> = {
  /** Permissionless caller */
  cranker: TransactionSigner<TAccountCranker>;
  /** Market (increments shards_ready) */
  market: Address<TAccountMarket>;
  /** Drained OrderSlab shard to zero for the next round */
  orderSlab: Address<TAccountOrderSlab>;
};

export function getResetShardInstruction<
  TAccountCranker extends string,
  TAccountMarket extends string,
  TAccountOrderSlab extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ResetShardInput<TAccountCranker, TAccountMarket, TAccountOrderSlab>,
  config?: { programAddress?: TProgramAddress },
): ResetShardInstruction<
  TProgramAddress,
  TAccountCranker,
  TAccountMarket,
  TAccountOrderSlab
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    cranker: { value: input.cranker ?? null, isWritable: false },
    market: { value: input.market ?? null, isWritable: true },
    orderSlab: { value: input.orderSlab ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("cranker", accounts.cranker),
      getAccountMeta("market", accounts.market),
      getAccountMeta("orderSlab", accounts.orderSlab),
    ],
    data: getResetShardInstructionDataEncoder().encode({}),
    programAddress,
  } as ResetShardInstruction<
    TProgramAddress,
    TAccountCranker,
    TAccountMarket,
    TAccountOrderSlab
  >);
}

export type ParsedResetShardInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Permissionless caller */
    cranker: TAccountMetas[0];
    /** Market (increments shards_ready) */
    market: TAccountMetas[1];
    /** Drained OrderSlab shard to zero for the next round */
    orderSlab: TAccountMetas[2];
  };
  data: ResetShardInstructionData;
};

export function parseResetShardInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedResetShardInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      cranker: getNextAccount(),
      market: getNextAccount(),
      orderSlab: getNextAccount(),
    },
    data: getResetShardInstructionDataDecoder().decode(instruction.data),
  };
}

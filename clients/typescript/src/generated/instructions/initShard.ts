/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ProgramDerivedAddress,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getResolvedInstructionAccountAsProgramDerivedAddress,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const INIT_SHARD_DISCRIMINATOR = 30;

export function getInitShardDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(INIT_SHARD_DISCRIMINATOR);
}

export type InitShardInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountOrderSlab extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountMarket extends string
        ? ReadonlyAccount<TAccountMarket>
        : TAccountMarket,
      TAccountOrderSlab extends string
        ? WritableAccount<TAccountOrderSlab>
        : TAccountOrderSlab,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitShardInstructionData = {
  discriminator: number;
  shardId: number;
  bump: number;
};

export type InitShardInstructionDataArgs = { shardId: number; bump: number };

export function getInitShardInstructionDataEncoder(): FixedSizeEncoder<InitShardInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["shardId", getU16Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_SHARD_DISCRIMINATOR }),
  );
}

export function getInitShardInstructionDataDecoder(): FixedSizeDecoder<InitShardInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["shardId", getU16Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

export function getInitShardInstructionDataCodec(): FixedSizeCodec<
  InitShardInstructionDataArgs,
  InitShardInstructionData
> {
  return combineCodec(
    getInitShardInstructionDataEncoder(),
    getInitShardInstructionDataDecoder(),
  );
}

export type InitShardInput<
  TAccountPayer extends string = string,
  TAccountMarket extends string = string,
  TAccountOrderSlab extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pays for the shard account */
  payer: TransactionSigner<TAccountPayer>;
  /** Market the shard belongs to */
  market: Address<TAccountMarket>;
  /** OrderSlab shard PDA to create (client resolves [b"order_slab", market, shard_id]) */
  orderSlab: ProgramDerivedAddress<TAccountOrderSlab>;
  /** System program */
  systemProgram?: Address<TAccountSystemProgram>;
  shardId: InitShardInstructionDataArgs["shardId"];
  bump?: InitShardInstructionDataArgs["bump"];
};

export function getInitShardInstruction<
  TAccountPayer extends string,
  TAccountMarket extends string,
  TAccountOrderSlab extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: InitShardInput<
    TAccountPayer,
    TAccountMarket,
    TAccountOrderSlab,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitShardInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountMarket,
  TAccountOrderSlab,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    market: { value: input.market ?? null, isWritable: false },
    orderSlab: { value: input.orderSlab ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!args.bump) {
    args.bump = getResolvedInstructionAccountAsProgramDerivedAddress(
      "orderSlab",
      accounts.orderSlab.value,
    )[1];
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("market", accounts.market),
      getAccountMeta("orderSlab", accounts.orderSlab),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitShardInstructionDataEncoder().encode(
      args as InitShardInstructionDataArgs,
    ),
    programAddress,
  } as InitShardInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountMarket,
    TAccountOrderSlab,
    TAccountSystemProgram
  >);
}

export type ParsedInitShardInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pays for the shard account */
    payer: TAccountMetas[0];
    /** Market the shard belongs to */
    market: TAccountMetas[1];
    /** OrderSlab shard PDA to create (client resolves [b"order_slab", market, shard_id]) */
    orderSlab: TAccountMetas[2];
    /** System program */
    systemProgram: TAccountMetas[3];
  };
  data: InitShardInstructionData;
};

export function parseInitShardInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitShardInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      market: getNextAccount(),
      orderSlab: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitShardInstructionDataDecoder().decode(instruction.data),
  };
}

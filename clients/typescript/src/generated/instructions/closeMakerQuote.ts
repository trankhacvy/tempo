/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TEMPO_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_MAKER_QUOTE_DISCRIMINATOR = 29;

export function getCloseMakerQuoteDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(CLOSE_MAKER_QUOTE_DISCRIMINATOR);
}

export type CloseMakerQuoteInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMaker extends string | AccountMeta<string> = string,
  TAccountMakerQuote extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMaker extends string
        ? WritableSignerAccount<TAccountMaker> &
            AccountSignerMeta<TAccountMaker>
        : TAccountMaker,
      TAccountMakerQuote extends string
        ? WritableAccount<TAccountMakerQuote>
        : TAccountMakerQuote,
      ...TRemainingAccounts,
    ]
  >;

export type CloseMakerQuoteInstructionData = { discriminator: number };

export type CloseMakerQuoteInstructionDataArgs = {};

export function getCloseMakerQuoteInstructionDataEncoder(): FixedSizeEncoder<CloseMakerQuoteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: CLOSE_MAKER_QUOTE_DISCRIMINATOR }),
  );
}

export function getCloseMakerQuoteInstructionDataDecoder(): FixedSizeDecoder<CloseMakerQuoteInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getCloseMakerQuoteInstructionDataCodec(): FixedSizeCodec<
  CloseMakerQuoteInstructionDataArgs,
  CloseMakerQuoteInstructionData
> {
  return combineCodec(
    getCloseMakerQuoteInstructionDataEncoder(),
    getCloseMakerQuoteInstructionDataDecoder(),
  );
}

export type CloseMakerQuoteInput<
  TAccountMaker extends string = string,
  TAccountMakerQuote extends string = string,
> = {
  /** Quote maker; receives reclaimed rent */
  maker: TransactionSigner<TAccountMaker>;
  /** Inactive MakerQuote PDA to close */
  makerQuote: Address<TAccountMakerQuote>;
};

export function getCloseMakerQuoteInstruction<
  TAccountMaker extends string,
  TAccountMakerQuote extends string,
  TProgramAddress extends Address = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
>(
  input: CloseMakerQuoteInput<TAccountMaker, TAccountMakerQuote>,
  config?: { programAddress?: TProgramAddress },
): CloseMakerQuoteInstruction<
  TProgramAddress,
  TAccountMaker,
  TAccountMakerQuote
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TEMPO_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    maker: { value: input.maker ?? null, isWritable: true },
    makerQuote: { value: input.makerQuote ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "omitted");
  return Object.freeze({
    accounts: [
      getAccountMeta("maker", accounts.maker),
      getAccountMeta("makerQuote", accounts.makerQuote),
    ],
    data: getCloseMakerQuoteInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseMakerQuoteInstruction<
    TProgramAddress,
    TAccountMaker,
    TAccountMakerQuote
  >);
}

export type ParsedCloseMakerQuoteInstruction<
  TProgram extends string = typeof TEMPO_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Quote maker; receives reclaimed rent */
    maker: TAccountMetas[0];
    /** Inactive MakerQuote PDA to close */
    makerQuote: TAccountMetas[1];
  };
  data: CloseMakerQuoteInstructionData;
};

export function parseCloseMakerQuoteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseMakerQuoteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { maker: getNextAccount(), makerQuote: getNextAccount() },
    data: getCloseMakerQuoteInstructionDataDecoder().decode(instruction.data),
  };
}

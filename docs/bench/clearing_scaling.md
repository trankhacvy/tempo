# Clearing host-benchmark — O(ticks), not O(orders)

Generated by `cargo run -p tempo-bench` (deterministic inputs; timings
are machine-dependent — the *shape* is the claim).

## Sweep 1 — runtime vs tick count (orders = 4×ticks)

| ticks | ns/find_cross |
|------:|--------------:|
| 64 | 3970 |
| 256 | 14511 |
| 1024 | 46199 |
| 4096 | 171138 |

## Sweep 2 — runtime vs order count (ticks fixed = 256)

| orders | ns/find_cross |
|-------:|--------------:|
| 256 | 11356 |
| 1024 | 11145 |
| 4096 | 11062 |
| 16384 | 11031 |

Sweep 1 grows ~linearly in ticks; Sweep 2 stays ~flat as orders grow 64× — the histogram method's defining property.

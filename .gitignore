.DS_Store
.env
.vscode/
/keypairs
/worktrees

# Rust
**/target/
**/coverage/
**/generated/
*.so

# Javascript / Typescript
**/node_modules/
**/dist/
/scripts/*.js

# AI
.claude/

# Solana Test Validator
**/test-ledger/
.runbook-logs/*

# Local
.local/

# CU tracking (keep the committed artifact in docs/bench/)
/cu_report.md

# Keyfiles
.keypairs/
.env*.local

# Multi-agent devnet simulation (agent secret keys + local RPC env)
.sim-keys/
.env.sim
apps/bots/.sim-keys/

# tempo-sim provisioner output: private agent keypairs + artifact — NEVER commit
/keys/
/sim-artifact.json
.env.sim.local

# Stress-test + LiteSVM sweep outputs (regenerated by scripts/sim/stress.sh + the sweep test)
stress-metrics.jsonl
stress.parquet
stress-*.png
sweep.csv
sweep-*.png
apps/bots/stress-metrics*.jsonl
apps/bots/stress.parquet
apps/bots/stress-*.png

# External reference codebases (read-only study material, not part of Tempo).
/percolator/
/percolator-prog/

# Trident fuzz target build artifacts (excluded crate).
/trident-tests/target/
/trident-tests/trident-fuzz/

# Phase 3 ops: deployment secrets (keypairs + local env) are never committed.
ops/compose/keys/*.json
ops/compose/.env

# Local reference clones (not part of this repo)
/rise-public/

# Personal research notes (not part of this repo)
/research.md
/research-vi.md
